# Project Memory

## Decisions
Current direction (2026-09-16 daily-log split): `PROGRESS.md` owns current state and coarse milestones; `worklog/YYYY-MM-DD.md` owns detailed work evidence. Earlier all-history-in-PROGRESS, HANDOFF, dual-write, clipping and freshness-reminder designs below are historical; do not reinstate them as current requirements.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-09-19 | Declined a reviewer's proposal to add a symmetric clause to `Execution` — "fix the verification method before building new features", mirroring the repro→fix→rerun bug loop. | `Goal Alignment` already requires the outcome, acceptance evidence and output shape before multi-step work, so the symmetry exists one section earlier and an `Execution` copy would be a restatement; the bug loop's object is code that already misbehaves, which is a different failure. Fails the admission test's duplication half with no new coverage. Revisit only if a replay shows feature work starting without a stated acceptance signal. [Evidence](worklog/2026-09-19.md#external-review-suggestions-triaged). |
| 2026-09-18 | Remove work-record content injection entirely (plan A): SessionStart is registered for Codex only and delivers just the protocol text; the agent reads `PROGRESS.md` itself at startup. Deleted `build_capsule`/`clip_field` and the field caps, and removed the Cursor/CodeBuddy SessionStart entries. | An injected excerpt is a second source of truth that can silently omit a constraint while looking authoritative; the 2026-09-16 "over-budget sections are omitted with source labels" safeguard existed only because injection existed. Removing injection removes the failure class and ~190 lines, and makes omission handling unnecessary. Codex keeps the hook because it has no `rules/` slot; Cursor/CodeBuddy/Claude load the canonical rule and need none. Supersedes the capsule-omission part of 2026-09-16, not the single-owner/no-handoff principle. |
| 2026-09-17 | Add proactive execution efficiency to the canonical rule: inspect likely waste before costly work or prolonged waiting, implement safe in-scope improvements, and preserve outputs, coverage and guarantees. | The user's cache-lock example shows that diagnosing "still running, not deadlocked" does not decide whether waiting is necessary. A report-only clause still leaves the user to initiate optimization. Keep the trigger always-on because an agent that misses the problem will not invoke an optimization skill; no new skill or polling hook. Explicitly admit bounded execution improvements under surgical edits, without licensing unrelated work or active-job interruption. Targeted checks, net benefit and stop conditions prevent an optimization project replacing the task. Work and unverified behavior: [evidence](worklog/2026-09-17.md#proactive-lossless-efficiency). |
| 2026-09-16 | Delete the repository-root `AGENTS.md` pointer; keep the protocol in the plugin rule and inject through host mechanisms. Do not ship a dev-repository pointer as the integration path. | The user corrected an earlier "keep" recommendation: a root `AGENTS.md` in the plugin's own repository only constrains that repository and does not give the capability to other projects. Non-plugin hosts need an entry point inside the target project, created only with user consent; when one exists, add a clearly delimited, independently removable section and preserve existing instructions instead of replacing or text-merging them. Capability must travel with the plugin, not with a file in its source repository. |
| 2026-09-16 | Split state from evidence: PROGRESS is a short goal/now/milestone map; dated worklog files hold task evidence. Keep task-relevant links in current sections, not a complete log index. Resume independently of date boundaries; hooks never scan daily logs. | The user values detailed work history but routine reads of a growing PROGRESS waste context. This supersedes the earlier same-day all-history-in-one-file design, not the single-owner/no-handoff principle. Write evidence first, then changed conclusions/links; no empty daily files, copied day snapshots, or automatic latest/yesterday reads. Original event dates and ambiguous ranges survive migration. Templates and migration detail stay in the skill; routing/recovery stays always-on because agents can write without invoking it. |
| 2026-09-16 | Make `PROGRESS.md` the sole work record, preserving current conclusions and dated results. Retire the handoff skill and mtime-based reminder; keep the independent large-code-file signal. SessionStart selects current progress sections, with read-only legacy fallback until `Current State` exists. | User values work history but finds a second snapshot redundant. Preserve useful legacy content before cleanup; never migrate in hooks or dual-write for old plugins. Over-budget sections are omitted with source labels instead of clipped into potentially false claims. This supersedes the earlier same-day dual-record proposal. Behavior is organized around gap → check → evidence → decision, with reassessment when effort expands or evidence stalls. Scope audit: keep navigation and omission handling always-on because agents can act without invoking skills; keep formats, migration details and replay scenarios on demand. Remove duplicate handoff and timestamp-triggered work rather than weaken verification or permission boundaries. |
| 2026-09-16 | Extend existing memory into a goal-alignment loop: `PROGRESS.md` owns the objective, success criteria, subgoal expectations and outcomes; `HANDOFF.md` carries an optional `Objective` summary, selected first by the capsule. Keep exploration/decision/stage behavior in the canonical rule, not a new skill or Stop gate. | User reports research drifting into branches and approving opaque recommendations. A new heading alone was reproduced as lost content on all three host script paths. Independent field caps preserve the goal without squeezing constraints; old handoffs need no migration. This supersedes the 2026-06-27 blanket ban on extra goal fields, not its anti-boilerplate intent. |
| 2026-09-16 | Bound process by decision value, not rigid task counts or goal trees. Important choices explain evidence/gap, expected contribution and cost; delegated research carries stop conditions; stage outcomes are reconciled. Remove redundant routing, forced empty files, automatic progress reads, repeated refresh approval and automatic backlog promotion. | The four new rule paragraphs address distinct failures (lost purpose, unbounded branching, opaque approval, task completion mistaken for success); each is scoped to multi-step research, substantial investigation, consequential choices or stage boundaries. They must stay always-on because agents can research and edit memory without invoking skills. Hooks can preserve text but cannot judge research value; behavior scenarios in the plugin README remain manual, and no total token saving is claimed. |
| 2026-09-10 | The milestone commit reminder is a protocol clause in `Execution`, not a stop-hook signal: at a validated milestone the agent recommends a commit and names what it would cover, commits only when the user asks, and raises it once per milestone rather than every turn. | Requested by the user so progress stays traceable. Admission test: (A) the artifact is a commit plus a visible recommendation in the reply; (B) no invocation-free mechanism covers it — the stop hook only fires a stale-handoff reminder, and per the 2026-08-27 decision its messages carry state, not protocol; (C) it replays against the 2026-09-02 failure, where a user-visible defect was misdiagnosed because the rule's uncommitted worktree text exonerated a clause `HEAD` still shipped — the corrective guidance there ("this repo carries large uncommitted rule edits for days") is the state a milestone commit removes. The hook was rejected as the carrier twice over: "milestone" is a judgment `git status --porcelain` cannot make, and a commit nudge on every dirty turn is exactly the every-turn-notification defect class rejected on 2026-08-27. Body 4235 → 4486, 286 past the review line, accepted under the user's standing instruction that the capability outranks the budget. |
| 2026-09-03 | CodeBuddy is a hybrid host: deliver the protocol through plugin `rules/` (the Cursor mechanism) and speak Claude-style hook JSON (the Codex mechanism). SessionStart injects the handoff capsule only — never the protocol. | The 2026-08-03 lesson is that packaging parity is not product parity. CodeBuddy IDE 4.5.0+ ships Rule as a plugin component and 4.7.2 recursively discovers plugin `rules/`; Codex's SessionStart injection exists because Codex has no rules slot. Using it on CodeBuddy would double-load the protocol on IDE. `plugin.json` has no `rules` field, so discovery is directory convention, not a manifest path. Stop stays advisory via `systemMessage`, never `decision: block`. |
| 2026-09-03 | CodeBuddy `plugin.json` `skills` must be an array of paths (`["./skills/"]`), even though the published schema says `string | array`. | `codebuddy plugin validate` rejected `"./skills/"` with `Field "skills" must be an array` (the only error; `hooks` as a string passed). Docs and CLI disagree; the CLI is what installs. Keep hooks as a string path to `codebuddy-hooks.json` so the default `hooks/hooks.json` (Cursor camelCase) is not loaded. |
| 2026-09-02 | Terminology fidelity is a language rule, not a preservation-list item: `Response Style` reads "Reply in the user's language, but write each term the way its field does." (A second half, "explain an unfamiliar one in one plain sentence rather than replacing it", was drafted here but deleted by the user as unnecessary; it never entered a commit. Shipped body 4235, not the 4309 first recorded.) | A user reported replies saying "金标" where the recommender-systems field writes "ground truth". Traced to a literal instruction, `translate borrowed terminology into the current project's terms`, added 2026-08-25 and still present in `HEAD` (`4429f3f`). The 2026-08-27 compression (982 → 450 chars) deleted the push but added no counterweight: the correctness-preservation list protects negation, conditions, numbers, units, paths, commands, and error text — every token type that must survive a rewrite except terminology. Adding "terms of art" to that list was the other option and was rejected: a preservation list can say a term must survive but not which form it must survive as, and the correct form depends on field usage, not language. Chinese recommender work writes 召回率 for recall and "ground truth" untranslated, so "keep English" would be wrong in the other direction. +70 chars, body 4309. |
| 2026-09-01 | Treat "guides better work" and "does not impose unnecessary work" as one design invariant, and enforce the second half where the first is enforced: every always-on clause must name a condition that turns it off on trivial work, and the validator's clause-audit prompt now asks that question alongside value and duplication. | Nothing in the plugin measured imposed work. Both stop-hook signals push toward more work (rewrite the handoff, split large files), the two size lines measure resident context rather than work, and the clause-audit prompt asked only "does it prevent a material failure" and "does it duplicate". Meanwhile the admission gate had just been broadened from "replays a recorded failure" to "prevents a predictable failure", making entry easier. Additions had a rationale and removals had no symmetric measure. Of 47 recorded decisions ~12 tightened and ~18 loosened, but 11 of the 18 landed in the last two days, so the balance was corrected late rather than maintained. |
| 2026-09-01 | Re-scope the two clauses left with unbounded cost, both introduced the same day: the bug loop applies to a behavior bug, not every bug fix; `Evidence` reads "never generalize from the first match: read the rest of the list or section it came from" instead of "read every source you cite to its end". | Replacing a vague hedge with no condition at all is not the same as replacing it with a checkable one, and the previous round conflated them. A clause-by-clause pass found 12 of 16 clauses carry an explicit off-switch (`non-trivial edits`, `materially change`, `proportionate to risk`, `outlives this turn`); the only two exceptions were these. "To its end" also licensed reading a 3000-line file to cite one function, while the 2026-08-13 incident it guards was generalizing from the first element of a sampled unit — so the bound belongs on the sampling unit, not the file. |
| 2026-09-01 | Make four clauses checkable instead of self-assessed: drop the bug loop's double hedge ("when feasible and proportionate" → unconditional, with an explicit unsafe/impossible escape); replace `Evidence`'s self-referential "inspect all relevant evidence" with "read every source you cite to its end" plus "run a search across the full scope claimed"; replace `Ownership`'s "likely to help future work" with "outlives this turn and its owner does not already carry"; state in `Ownership` that every file is optional. | Each original relied on the agent grading its own judgment — the exact failure mode already recorded for the compaction rule, where self-assessment is graded leniently under context pressure. A hedge placed before an instruction supplies the excuse to skip it. Optionality was contradictory: `Architecture Checkpoint` said "when present" while the `Ownership` table read as seven required files, and `bootstrap-context` may omit some. |
| 2026-09-01 | Accept the always-on rule at 4204 chars, 4 past the review line, rather than reword for 4 characters. | The line is advisory and asks for a clause audit, which was done: two clauses were tightened with no information lost, from 4243 to 4204. Squeezing the last 4 chars would repeat the recorded failure where budget pressure degraded clarity and produced "covered by another clause" rationalizations. |
| 2026-09-01 | Restore the missing Karpathy goal-driven bug loop: when feasible and proportionate, capture a bug with a minimal reproduction or failing check before fixing it, then rerun the same check; name substitute evidence when reproduction is unsafe or unavailable. | The upstream `multica-ai/andrej-karpathy-skills` rule says `"Fix the bug" → "Write a test that reproduces it, then make it pass"` and its examples explicitly run the same test red then green. This project recorded adopting observable success signals, surgical changes, and verifiable next actions, but omitted the same-evidence-before/after behavior while avoiding rigid Goal/Verify fields. Generic post-change validation does not prove the reported failure existed or that the fix removed it. |
| 2026-09-01 | Replace the overloaded `Safety` section with `Execution` and `Evidence`; keep `Response Style` separate. | `Safety` mixed authorization, defect repair, edit scope, validation, source completeness, epistemic status, and secret handling. The label made normal engineering judgment sound hazardous and obscured the two actual concerns. `Execution` now owns how work is changed and verified; `Evidence` owns how conclusions are supported. Response-style clauses still govern output, including full explanation of security and irreversible actions, so none moved. |
| 2026-06-26 | Keep project memory in `.agent-context/` markdown files, one canonical owner per fact. | Persistent files are the product: project understanding, accumulated experience, and lessons survive chat context loss. Single ownership avoids split writes and missed reads. |
| 2026-06-26 | Drop cross-review from the plugin; the product is durable agent context, not review. | Cross-review diluted the direction and pulled the plugin toward a second job it did worse. |
| 2026-06-26 | Cover agent work habits, not only stored facts; token savings are secondary. | The user's goal is AI-assisted coding that resumes cleanly, not a smaller prompt. (Later bounded by the 2026-08-27 admission test, which stopped this from justifying open-ended behavioral prose.) |
| 2026-06-26 | Keep always-applied rules minimal; detailed templates live in skills. | The core rule is injected every session while skill bodies are free until invoked. Became the 3600-char budget on 2026-08-27. |
| 2026-06-26 | Add a conservative `stop` handoff reminder instead of a mandatory completion workflow. | A lightweight signal can catch stale handoffs without routing every turn through a heavy process. |
| 2026-06-26 | Trim confirmation guardrails to judgment risks only. | Host tool approvals already cover mechanical permissions, so the plugin should ask about ambiguous intent, architecture, core behavior, critical config, and destructive operations. |
| 2026-06-27 | Borrow Karpathy-inspired ideas selectively rather than copying the full guideline set. | agent-context should stay focused on durable project memory and lightweight operating protocol. |
| 2026-07-01 | Improve modular-design behavior via an objective signal loop + anti-over-split guard, not more prose or a new heavy skill. | Prose conventions are easily ignored under context pressure and self-assessment is lenient; a measurable stop-hook signal plus a pre-edit checkpoint is lighter and harder to skip. |
| 2026-08-01 | Prefer dual-manifest packaging for Codex (same plugin folder: `.cursor-plugin` + `.codex-plugin`) over a separate fork. | Skills and `.agent-context/` protocol are shared; only rules packaging and hook I/O differ. Avoid maintaining two product trees. |
| 2026-08-03 | Borrow OpenAME's dual-manifest + dual-hooks packaging shape; do not copy their fork-skeleton, multi-plugin marketplace, or giant AGENTS.md workflow. | OpenAME already solved CodeBuddy↔Codex packaging; agent-context only needs the thin packaging layer for one plugin. |
| 2026-08-03 | Eliminate host differences in shared content instead of managing them: no generator, no second copy. | The only real divergence was hardcoded invocation syntax (`/name` vs `$name`). Removing it makes one copy serve both hosts, so OpenAME's generator becomes unnecessary at N=1. |
| 2026-08-03 | Keep the protocol in exactly one file and let every host read that file. | Cursor loads it as a rule, the Codex hook injects it, `AGENTS.md` points at it. Copies had already drifted within days of being made. |
| 2026-08-03 | Stop-hook signals stay advisory on both hosts: Cursor `followup_message`, Codex `systemMessage`. Never Codex `decision: block`. | The staleness signal fires on most working turns, so a forced continuation would roughly double turn count for a state that is harmless mid-session. Hooks inform; the protocol's update triggers instruct. |
| 2026-08-03 | Compute the two stop signals in separate functions even though they share one emission. | They differ by orders of magnitude in firing rate; coupling them meant retuning the noisy one would silently drop the valuable one. |
| 2026-08-03 | Keep the name `agent-context`; differentiate through the description and keywords instead of renaming. | Checked eight candidates (cairn, engram, throughline, warm-start, carryon, binnacle, memory-bank, agentmemory) and every one is taken, most by a direct competitor. A rename relocates you behind a different squatter; it buys no discovery. |
| 2026-08-03 | Trim the always-applied rule by structural deduplication only; leave `Safety`, `Architecture Checkpoint`, and `Hygiene` at full length. | Those three govern behavior, and agents frequently write `.agent-context/` files directly without invoking the matching skill, so the always-on copy is the only thing steering that path. Terser behavioral prose buys tokens at the cost of compliance. |
| 2026-08-03 | Rename the memory directory from `.agent/` to `.agent-context/`, matching the plugin name. | `.agent/` never hard-collided with a harness, but it sat one character from the ecosystem's `.agents/` and this repo root held both at once, which invites misdirected writes by humans and models alike. Owner-namespaced directories cannot collide. Done pre-release at 91 references, when it was cheapest. |
| 2026-08-05 | Import only the epistemic axis from a shared third-party global prompt (mark verified vs assumed, give open questions an owner, state a task's finish condition); reject its long-termism/elegance/communication sections. | The protocol governs where a fact goes and when to write it, never whether the fact is trustworthy — that was the one real gap. The rejected sections produce no memory artifact and no observable output, and adopting them would reverse the 2026-06-27 decision to stay a memory protocol rather than a guideline collection. |
| 2026-08-13 | Extend the epistemic axis from memory writes to asserted conclusions: enumerate the source's contents before concluding, and back a quantified claim with a computed number or mark it an impression. | The 2026-08-05 clause guards `.agent-context/` entries and pre-edit source checks, so a claim that never touches a file was ungoverned — which is where the costly hallucinations happen. Accepted as a narrow exception to the 2026-06-27 "memory protocol, not a guideline collection" boundary because it is checkable, not aspirational. |
| 2026-08-25 | Add a short response-style section to the canonical protocol for both hosts. | The user found agent replies verbose and hard to read. The rule sets a stable information order, plain-language requirements, and accuracy exceptions without copying Caveman's broken grammar or blanket removal of uncertainty words. |
| 2026-08-27 | Make the default fix scope the defect class: sweep sibling call sites, parallel modules, other UI surfaces, and each host before declaring a fix done; repair same-root-cause instances that are equally safe, list the rest and ask. | The old `Safety` wording ("every changed line must trace to the request") was satisfied by fixing only the spot the user pointed at, so the same bug survived in every other place it lived and the user had to find those manually. "Unrelated" is now defined by root cause, not by whether the user named the location. Kept the ask-before-risky half so the clause does not become a license for scope creep. Always-on cost +393 chars (~98 tokens by the chars/4 estimate; not tokenizer-measured). |
| 2026-08-27 | Judge always-on clauses by an admission test instead of patching them one line at a time: a clause stays only if it (A) produces a memory artifact or observable output, (B) has no invocation-free mechanism already covering it — host prompt or this plugin's own hooks, skills excluded per 2026-08-03 — and (C) replays against a recorded failure it would have caught. | The rule had grown from 2493 chars at `2f59826` ("reduce always-on token use") to 4368, +75% across five commits, four of them additive prose. Applying the test: `Safety` scores 5 C-hits and keeps its full length, while `Response Style` (23.2%) and `Architecture Checkpoint` (9.2%) scored zero across the 10 recorded incidents. Judging clause by clause is what separated the 26% that earns its size from the 32% that does not. |
| 2026-08-27 | Settle the unrecorded conflict between 2026-08-05 and 2026-08-25: keep 08-05's principle, drop 08-25's implementation. `Response Style` shrinks 982 → 450 chars, keeping only reply-in-the-user's-language (host prompts do not require it), the mandatory-recommendation clause, and the correctness-fidelity counterweight; the epistemic line moves into `Safety` with the other two. | 08-05 rejected communication guidance because it "produce[s] no memory artifact and no observable output", then 08-25 added 982 chars of exactly that — the largest single addition in the file's history — without noting the reversal. Both cannot stand. The section also failed in practice: its unconditional "Remove pleasantries… Do not restate the user's question" collided with its own soft problem/impact/recommendation slot, and under pressure the mandatory cut beat the droppable third slot. |
| 2026-08-27 | Reverse the 2026-08-03 "structural deduplication only; leave `Safety`, `Architecture Checkpoint`, and `Hygiene` at full length" for the latter two: cut 219 chars of split-by-responsibility prose and the two hygiene bullets the stop hook already states. | The 08-03 reason was that skill overlap is not coverage, because agents write `.agent-context/` files without invoking the matching skill. That reason is untouched and was not used here. These cuts rest on a different mechanism: hooks fire without invocation. `large_file_note()` says "split by responsibility… do not over-fragment" at `LINE_THRESHOLD = 600`, and the handoff signal says "if there is no active task, say so" — both at the moment the state actually matters, which is what the 2026-07-01 decision asked for. |
| 2026-09-01 | Restore five clauses that had been shortened or deferred under the old cap, at +100 chars. | The user asked directly whether the budget had cost any clarity, and it had, in four places. `Scale to the request` had lost its object and pointed at "a checkpoint" rather than the `Architecture Checkpoint` section it scopes. The confirmation exception had lost "explicitly", which widened it: a vague request like "make it better" would have counted as asking for a core behavior change. `Startup` still said "run `bootstrap-context` first" and relied on the reader inferring the qualifier from the proportionality clause two sections away — now "before substantive work". `Response Style` was left unscoped on the argument that proportionality covered it, which was only partly true: it covers an invented next step for a question but not a recommendation forced onto each finding of a purely explanatory answer — now "When you report findings, risks, or options". Also replaced the ambiguous "enumerate what the source actually contains" with "read all of what the source contains, not the first matching part", which states the 2026-08-13 failure it replays. Re-checked the four deletions that paid for the proportionality clause against a no-budget standard: all four were duplication or misplaced prescription, so none needs restoring. |
| 2026-09-01 | Size is advisory at both review lines: warn past 4200 and warn more strongly past 6000; only objective structure or drift failures block validation. This supersedes the same-day 6000-char error. | A character count is only a proxy for instruction competition, so it cannot prove a capability is wrong. The former 6000 justification compared unlike categories ("the rule exceeds the rest of the injection") and still let the proxy block the product after the user explicitly rejected that priority. The strong warning now asks why the capability must stay always-on; it does not forbid the answer. Negative-tested: both tiers warn and exit 0. |
| 2026-09-01 | Scope the core protocol to preserve agent autonomy: inspect relevant evidence rather than all source; search likely same-root-cause locations proportionate to risk; ask only for a missing material user choice; validate proportionate to risk; consult optional memory files only when present/current, with source and project configuration authoritative. | A review found that the rule still converted safeguards into exhaustive work: "read all" had no boundary, the defect sweep said both "scale" and "every host", architecture choices required confirmation even when the agent could safely decide, and `ARCHITECTURE.md`/`COMMANDS.md` were mandatory although bootstrap intentionally may not create them. These changes preserve the failure each clause targets without prescribing the maximum process every time. |
| 2026-09-01 | Memory uses one canonical owner, not a ban on repetition; summaries are allowed when resumption needs them. Remove start-time HANDOFF/PROGRESS writes and require updates only for material state changes or handoff moments. | A self-contained handoff necessarily summarizes facts owned elsewhere, so literal "do not duplicate" conflicted with the capsule design. Start-time writes usually record no durable change and turn the protocol into bookkeeping. |
| 2026-09-01 | Broaden the clause admission test from "replays a recorded failure" to "prevents a material observed or predictable failure". | Requiring a prior incident rejects legitimate preventive safeguards and biases the plugin toward learning only after harm. The review warning still demands a concrete failure mechanism and rejects aspirational prose; it no longer requires that the project has already suffered it. |
| 2026-08-27 | *(superseded 2026-09-01)* Cap the always-applied rule body at 3600 chars and assert it in `scripts/validate-template.mjs`, scoped to any rule with `alwaysApply: true`. | Prose asking for restraint is the thing this project has repeatedly found inert; a ceiling turns each future addition into a displacement decision. The check measures the body the way both hosts consume it, mirroring `read_protocol()` in `session-context.py`, and 3600 leaves 105 chars of headroom over the post-audit 3495 — enough for wording fixes, not for a new section. |
| 2026-08-27 | Replace whole-file handoff injection with a fixed-size resume capsule: select `Current Task`, `Status`, `Next Action`, `User Instructions`, `Validation`, `Blockers`; cap each field separately (200-400 chars, `Next Action` largest); total backstop 1700; drop `Validation` then `User Instructions` if over. | Measured injection was 4363 chars (~1090 tokens by chars/4), larger than the 3495-char rule it accompanied, and `HANDOFF_MAX_CHARS = 4000` sliced the file's tail — losing blockers, user constraints and notes, which sit last. Per-field caps beat one total cap because the fields are not equally compressible; a clipped `Next Action` defeats the capsule's purpose. Cursor 4363 → 1459, Codex 7800 → 5061. |
| 2026-08-27 | Keep user constraints and validation gaps in the capsule rather than the four "core" fields alone. | Reviewer's correction, accepted: this repo's own `User Instructions` held two standing user rules ("replies must carry recommendations", "do not patch the rule line by line"). A capsule that drops them re-creates the exact failure the constraints were written to prevent. Ranked droppable-last instead of excluded. |
| 2026-08-27 | Hook messages carry state only; the protocol owns every instruction. Removed the session-start trailer that restated `Startup` and the two branch messages that restated `bootstrap-context` and the load-on-demand order. | The 2026-08-03 decision says the protocol lives in exactly one file and nothing else may restate it, because copies drifted within days. The hook prose was a second copy, and the validator's host-neutrality assertion could not see it. |
| 2026-08-27 | Rejected two of the reviewer's suggestions and one of my own from the same review: no stop-hook size signal for `.agent-context/`, no date-based `last verified` warning. | The size signal would add another every-turn notification — the same defect class the review was diagnosing; file-size pressure belongs in the skill that already reads the file. A date warning produces ritual date-bumping, so `ARCHITECTURE.md` now records `verified against: <commit>` plus the paths the map covers, which a later agent can actually check. |
| 2026-08-27 | Deferred, not rejected: splitting `Architecture Checkpoint`, the defect-class sweep, `Response Style`, and the large-file signal into an optional quality layer. | The diagnosis is sound — the plugin has grown past "durable memory" into general agent governance. But a second Cursor rule file is also always-applied, so the split only saves tokens if it ships off by default, which is a product-direction change and conflicts with the one-file decision. Needs the user's call, not a silent refactor. Asked and deferred again 2026-09-01: the user chose to measure first, since the per-clause off-switches added that day may already remove the overhead the split was meant to remove, and a layer split is an irreversible structural change to make on data rather than on argument. Unblock it with the contrast test in `HANDOFF.md`. |
| 2026-09-01 | Add a proportionality clause to the protocol header — `Scale to the request: a question wants an answer, not a checkpoint, a memory write, or an invented next step.` — and pay for it by deleting four redundant or over-prescriptive clauses. | A capability audit found 28 of 33 clauses unconditional (85%), so the protocol applied its full weight to a factual question: name the owning module, attach a recommendation per finding, end with a next step, decide whether to write memory. Proportionality had never been considered in any recorded decision. It also subsumes two separate findings: `bootstrap-context` no longer front-loads seven files onto an unrelated question (a bootstrap is a memory write), and `Response Style` no longer manufactures a next step for an informational reply — so neither needed its own clause. Net −2 chars, 3566/3600. |
| 2026-09-01 | Deleted from the always-applied rule: `Keep dependency direction one-way; avoid cycles` (universal architecture prescription; some cycles are legitimate, and `CONVENTIONS.md` owns project style), `Store latest results in HANDOFF.md, reusable rules in COMMANDS.md` (restates the Ownership table), `Files live in .agent-context/` (the header and `Startup` already name it), and `Rewrite it` from the hygiene bullet (the table already gives the rewrite triggers). Changed `Do not guess behavior` to `Do not assert unverified behavior`. | The four deletions are duplication or generic design advice, not memory protocol. The `guess` rewording is the capability fix: taken literally the old wording banned hypothesis formation, which is how debugging works, while what it meant to ban — presenting a guess as verified — is already owned by `Distinguish facts, assumptions, and recommendations`. |
| 2026-09-01 | Scope the confirmation list with `the requested change itself needs no re-approval` rather than shortening the list. | The list of topics was already deliberately trimmed on 2026-06-26; the gap was that a topic appearing on it says nothing about whether the user had already decided. Most substantive requests *are* core behavior changes, so read literally the clause asks permission for the thing just requested. Scoped to the requested change itself, so side effects the user did not ask for still require asking. |
| 2026-09-01 | Keep the surgical-edit clause's "do not fix unless asked" half, but raise its reporting half from "Mention issues with a different root cause" to "Report issues with a different root cause, with locations". | The user read the old wording as guiding the agent to stay quiet, and the emphasis did point that way: the sentence's weight sat on the prohibition while the disclosure duty was one weak verb. It also created a two-tier standard the second tier of which was unfalsifiable — the adjacent same-defect clause demands "report every occurrence... with locations", so an agent could satisfy "mention" with "there may be other issues". `Response Style` already requires a recommendation per finding, so the clause supplies the location and does not restate that. Deleting the sentence was the other option offered and was rejected: the prohibition has a recorded rationale (2026-08-27, scope-creep guard). +15 chars, 3568/3600. |
| 2026-08-27 | The capsule must be sufficient on its own: a clipped field gets `…` and no instruction, and only a section the capsule never carries earns a pointer to the file. | A reviewer did the arithmetic I had skipped: capsule 1399 + the full 2936-char file, which my own wording invited on almost every session, is 4335 against the old 4363 — a 28-char saving, not the 728 tokens I reported. A budget that assumes the agent will not read the file is not a budget. Added `Notes for Next Agent` to the carried fields for the same reason. |
| 2026-08-27 | Hook behaviour gets a committed test file (`scripts/test-hooks.py`, stdlib `unittest`), not inline throwaway scripts. | Every capsule guarantee I claimed rested on commands typed into a shell and then lost. The suite found two real bugs in its first run and each of the three new invariants was negative-tested. Fits the stdlib-only positioning: no dependencies, ~1s, builds a throwaway git repo per test. |
| 2026-08-27 | The template/capsule contract lives in the test suite, not in `validate-template.mjs`. | The reviewer suggested the validator. But the test imports `CAPSULE_ORDER` from the module, while the validator is JavaScript and would have to regex the constant out of Python — a second, weaker definition of the same invariant, which is the drift pattern the 2026-08-03 decision exists to prevent. |
| 2026-08-03 | Position on what the project does not have: no index, no database, no daemon, no MCP server, stdlib-only hooks, one source for both hosts. | Competitors ship knowledge graphs (engramx), 29-tool MCP servers (cairn), local Postgres + pgvector (throughline), 93 REST endpoints (agentmemory). Smallness is the only unoccupied axis in this category, and it is what this project actually is. |

## Lessons Learned
| Date | Type | Lesson | Corrective Guidance |
|------|------|--------|---------------------|
| 2026-09-17 | failure | Two Codex budget tests compared `len(message)` (characters) against `additionalContextLimit`, an approximate-token threshold. The error stayed invisible until rule growth made the worst-case legacy pair fail at 11396 vs 10000, and the failure then looked like the new section's cost. | Confirm a limit's unit at the source before asserting on it. Codex: `codex-rs/utils/string/src/truncate.rs` — `approx_token_count` = `ceil(UTF-8 bytes / 4)`; `codex-rs/hooks/src/output_spill.rs` spills over-limit output to disk with a truncated preview. Measure bytes, not characters, and negative-test one unit past the boundary. [Evidence](worklog/2026-09-17.md#codex-budget-unit-correction). |
| 2026-09-10 | lesson | `MEMORY.md` and `PROGRESS.md` recorded the 2026-09-02 terminology clause as shipped in its two-part form at body 4309, but the tracked rule is 4235 and carries only the first part; the user had since deleted the second part as unnecessary. The two-part text survived only in the installed copy, which read as evidence the record was right. Records corrected 2026-09-10. | A `.agent-context/` entry that quotes rule text is a claim about `HEAD`, not about the worktree or `~/.cursor/plugins/local/`. Quote it from `git show HEAD:<file>` when recording it as shipped, and re-check the quote when a later round edits the same line. |
| 2026-09-03 | lesson | A Claude-shaped runtime is not automatically a Codex-shaped product. CodeBuddy's hook envelope matches Codex, but it has a rules slot Codex lacks, so copying the Codex SessionStart path would have shipped a silent double injection on IDE. | When adding a host, split "how the hook talks" from "which component carries the protocol". Use the host's rules slot when it has one; the Codex injection path is the fallback for hosts without one. |
| 2026-09-02 | failure | A user-visible output oddity was diagnosed by reading the rule's current text, which no longer contained the cause. The instruction that produced it, `translate borrowed terminology into the current project's terms`, had been compressed out of the worktree but survives in `HEAD`, so the working copy exonerated a clause the last commit still ships. | When output behavior looks wrong, diff the clause across `HEAD`, the worktree, and the installed copy before concluding, because this repo carries large uncommitted rule edits for days. `git show HEAD:<rule>` answers it in one command. |
| 2026-09-02 | lesson | Deleting a harmful instruction is not the same as preventing the behavior it caused. The 2026-08-27 compression removed "translate borrowed terminology" but left terminology as the only correctness-relevant token type absent from the preservation list, so nothing opposed the behavior once the push was gone. | When a compression round deletes a clause that pushed a behavior, ask whether anything now opposes that behavior. If the answer is no, the deletion needs a counterweight, not just an absence. |
| 2026-09-01 | failure | Editing the protocol's write triggers left five stale restatements: `handoff/SKILL.md` description + body, `update-progress/SKILL.md` (3 lines incl. the `touched files` field retired on 2026-08-27), `COMMANDS.md` behavioral test, and `README.md`. Two survived an earlier sweep of the same class. | When a clause in the protocol changes, grep the old wording repo-wide before claiming the edit is done — skills, `README.md`, and `.agent-context/` all restate it. Prefer pointing at the protocol's `Ownership` table over restating triggers. |
| 2026-09-01 | lesson | A grep-based invariant is worth more than another manual sweep, but its first version flagged the very clause that forbids the retired field, and its scope missed `README.md`. | Assert retired vocabulary per line with an explicit allowance for the prohibition, and include every always-on or user-facing surface in the scan. `scripts/test-hooks.py::test_retired_handoff_field_stays_retired`. |
| 2026-06-26 | failure | Plugin edits do nothing until the local plugin is reinstalled and the host reloads. | Run `./scripts/install-local.sh`, reload Cursor, and read the installed copy before believing a behavior change. |
| 2026-06-26 | lesson | New-chat continuity needs an active handoff, not only project progress. | `PROGRESS.md` carries project state; `HANDOFF.md` carries next action, validation, blockers, and user instructions. |
| 2026-06-26 | lesson | File names shape agent behavior: a narrow name makes agents withhold facts that belong in the file. | Prefer broad names — `MEMORY.md` for decisions, lessons, failures, and corrections alike. |
| 2026-06-26 | lesson | `.agent-context/` files grow forever unless something compacts them. | Summarize old progress and consolidate repeated lessons on every sync. Repeatedly violated since; see the 2026-08-27 entry on why prose alone does not enforce this. |
| 2026-06-26 | lesson | End-of-session handoff reminders must be conditional, fail open, and cap their retries. | Fire only when changed files look newer than `HANDOFF.md`. Refined 2026-08-27 to exclude `.agent-context/` entirely. |
| 2026-06-26 | failure | A new hook script stayed non-executable because `scripts/install-local.sh` hard-coded only the original one. | When adding a hook script, make the install script chmod every script it copies, not a named list. |
| 2026-06-26 | failure | A stop hook that ignores turn status overrides the user's own cancellation. | Emit `followup_message` only for `status: "completed"`; return `{}` for aborted and error turns. Asserted in `scripts/test-hooks.py`. |
| 2026-09-01 | lesson | My char-cost estimate for a five-clause rule edit was off by 2x (+43 predicted, +91 actual) and the validator rejected the result. | Do not quote a budget delta from mental arithmetic on prose. Draft the replacement text, measure it, then decide what to displace. The budget worked exactly as intended here: being blocked forced 67 chars of real duplication out of the file, and the final result added a clause while ending 2 chars smaller. |
| 2026-09-01 | lesson | Leaving the rule at 3599/3600 would have been worse than failing. One char of headroom reads as a broken ceiling and invites the next agent to raise it. | When a budget is nearly exhausted after a change, keep cutting until there is usable headroom, or it stops being a displacement mechanism and becomes a number to edit. Superseded the same day by the two-tier trigger, which removes the cliff this lesson was working around. |
| 2026-09-01 | lesson | A tight budget does not only block additions; it degrades the additions that fit. Under the old cap I shortened a new clause into vagueness, dropped a qualifier that widened a safety exception, and twice argued a fix was "already covered" by a clause two sections away — an argument I would not have made with room to spare. | "Covered by another clause" is a claim to verify, not a way to fit under a limit. Check it by reading the two clauses in isolation and asking whether the second one, alone, forbids the failure. If a size limit is what makes compression attractive, record what was cut so it can be restored when the limit moves. |
| 2026-09-01 | lesson | A guardrail whose stated reason is false will eventually be enforced against the goal it was meant to serve. The rule budget was justified as a cost control; measurement put the cost at 0.45% of the context window, so the real reason was attention, and nobody noticed the mismatch until it started blocking product work. | State a limit's actual mechanism of harm in the code that enforces it, and put a number next to the claim. A guardrail with a measured premise can be re-derived when conditions change; one with a plausible-sounding premise just accumulates authority. |
| 2026-09-01 | lesson | A clause can carry a duty and still teach the opposite, because a skimming reader keeps the imperative and drops the qualifier. "Mention issues…; do not fix them unless asked" literally required disclosure, yet read as permission to stay quiet. | When two halves of a clause pull opposite ways, match their verb strength and specificity to the priority you actually want. Check a new clause against the strongest neighbouring clause on the same axis, not only against its own wording. |
| 2026-08-27 | failure | One data format was defined in four places and three of them disagreed within the same hour: the rule and `sync-context` still listed "touched files" for `HANDOFF.md` while the `handoff` skill forbade it, and `bootstrap-context` listed a "freshness" section the template no longer had. I had edited the template and only one of its three descriptions. | A format needs one owner — the `handoff` skill's template — and the others must point at it rather than restate the field list. `scripts/test-hooks.py` now asserts the template defines every field the hook injects, so this specific drift cannot return silently. |
| 2026-08-27 | lesson | A dead code path reads as a working safeguard. The capsule's drop-order could never fire (per-field caps summed under the total cap) yet it was documented as the overflow behavior — and what it would have dropped was `User Instructions`, contradicting a decision recorded an hour earlier. | Prefer an invariant asserted in a test over a runtime fallback: the caps now bound the total by construction and `test_worst_case_stays_under_the_backstop` fails if a future cap breaks that. |
| 2026-08-27 | lesson | Git collapses an untracked directory into a single `?? dir/` entry, so a test fixture built with `git init` and no commit cannot tell which file a turn touched. | Commit the fixture before asserting on `git status --porcelain`. Cost an initial false pass in the stop-signal tests. |
| 2026-06-27 | Product guidance | User value is easier to perceive when README names observable success signals. | Prefer "How to know it's working" style signals over only describing internal files. |
| 2026-06-27 | Handoff design | Rigid Goal/Verify fields may become low-signal boilerplate. The blanket field ban is superseded by the 2026-09-16 goal-continuity decision above. | Keep verification in the next action, not a separate Verify form. The optional `Objective` field has a tested resume role; do not generalize it into mandatory planning scaffolding. |
| 2026-08-03 | failure | Porting the package to a second host is not the same as porting the product. The first Codex package shipped skills and hooks but no protocol, because Codex has no always-applied rules slot — it silently degraded to "a hook that prints HANDOFF.md". | When adding a host, first ask which component carried the value on the original host, then find that host's equivalent mechanism. Packaging parity proves nothing. |
| 2026-08-03 | lesson | A passing validator can mean the new checks never ran. | Negative-test every new assertion in a throwaway copy: break the thing, confirm the error, restore. |
| 2026-08-03 | lesson | Duplicated content drifts within days, and the drift can look legitimate. | Prefer a single copy plus an automated invariant over two copies plus discipline. Assert the invariant in the validator so it cannot rot. |
| 2026-08-03 | lesson | A signal that fires on almost every turn is usually measuring the wrong moment, not using the wrong threshold. Handoff staleness mid-session is harmless; it only costs anything when context is about to be lost. | Before tuning a threshold, ask when the measured state actually causes harm, and whether the hook fires at that moment. |
| 2026-08-03 | lesson | In a saturated category a distinctive name is not a differentiator, because every good noun is already owned by a competitor. The differentiator is the axis nobody occupies. | Search the category before renaming for distinctiveness. If the name space is exhausted, spend the effort on the one-line positioning and the `keywords` array, which is where marketplace search actually looks. |
| 2026-08-03 | failure | Recorded the leftover `[plugins."agent-context-codex@personal"]` entry in `~/.codex/config.toml` as "harmless, Codex skips unresolvable entries". It was not harmless: because the marketplace now declares `agent-context`, Codex only ever looked for the dead name, the real plugin was never enabled, and the protocol silently never reached any Codex session. Codex had been logging `configured non-curated plugin no longer exists in discovered marketplaces` all along. | A stale enable-entry is not inert; it can be the reason the working thing is never enabled. When a plugin does not appear in the host UI, read the host's own logs before inspecting your own files. Codex logs to `~/.codex/logs_2.sqlite`, table `logs`, column `feedback_log_body`. |
| 2026-08-03 | lesson | Codex has no always-applied rules mechanism, so an `.mdc` file means nothing there; the protocol reaches the model only as `additionalContext` from the plugin's SessionStart hook. "Is the rule in effect?" reduces to "is the plugin enabled, are its hooks trusted, did SessionStart fire?" | Verify Codex behavior at the host level (plugin enabled, hook `statusMessage` visible, logs clean), not by inspecting rule files. |
| 2026-08-03 | failure | Estimated that merging the duplicated `Ownership` and `Update Triggers` sections would cut the always-on rule ~25%; the real result was 9% (790 to 721 tokens). Two sections encoding the same mapping looked half-redundant, but the redundancy sat in the four cheapest bullets (`Commands/checks -> COMMANDS.md` and friends, under 10 tokens each), while the two expensive ones carried real event triggers that had to be kept. | Size the specific lines you intend to delete before promising a percentage. Do not price a refactor off the total size of the sections involved. Also beware rewriting compact slash lists into prose during a merge — the first attempt gave back most of the saving that way. |
| 2026-08-03 | lesson | Only the always-applied rule and the skill `description` lines cost tokens every session; skill bodies are free until invoked. Here that is ~810 always-on against ~1600 on-demand. | When asked to shrink prompts, measure the always-on tier first and leave on-demand content alone; compressing skill bodies optimizes something nobody pays for in the common case. |
| 2026-08-03 | lesson | Generic directory names collide by adjacency, not by exact match. No harness ever claimed `.agent/` (they took `.cursor/`, `.codex/`, `.claude/`), yet it sat one character from `.agents/`, the ecosystem's namespace for skills and plugin marketplaces, and this repo root contained both at once. OpenAME alone references `.agents/` 109 times. | Namespace a directory by its owner, mirroring `.cursor/` and `.github/`, rather than by its genre. Genre names are the ones every tool wants, so they are the ones that collide. |
| 2026-08-03 | failure | Removing the stale config entry was necessary but not sufficient, and the follow-up advice ("enable it in the UI") was wrong: the plugin never appeared in the UI because its status was `not installed`, and that list only renders installed plugins. Copying files to `~/.codex/plugins/` and declaring them in `~/.agents/plugins/marketplace.json` only makes a plugin *discoverable*; `codex plugin add <name>@personal` is what snapshots it into `~/.codex/plugins/cache/<marketplace>/<name>/<version>/` and enables it. Codex runs the cached copy, never the source. | Do not diagnose a host integration through its GUI when the host ships a CLI. `codex plugin list` names the state directly (`not installed` vs `installed, enabled`) and would have ended this in one command. Install scripts must perform the activation step, not print it as a manual instruction. |
| 2026-08-03 | lesson | `codex plugin add` re-copies the source even when the version is unchanged, so it is safe to run unconditionally on every local install. | Re-run it after every plugin edit; bumping the version is not required to refresh the cached snapshot. |
| 2026-09-18 | failure | During a capability review, the agent running under this protocol derailed into `pip install tiktoken` to settle a token-count question it had itself flagged as unverified in earlier turns. Three present clauses did not fire: "Stop at sufficient evidence", "reassess value/cost when effort grows materially" (failed import → install escalation), and "report checks that cannot run" (correct default after the first failure). First direct evidence for the 2026-08-05 open question that epistemic clauses are inert under context pressure. | Stating an unknown declares a boundary, not a backlog item; a failed check's default is report-and-estimate, never escalate environment changes for off-decision-path questions. The header's "invented next steps" clause governs answers, but an agent's own recommended next step can become an execution queue when the user later says "继续" — treat self-recommended steps as requiring the user to name them, not mere continuation. [Evidence](worklog/2026-09-18.md#protocol-self-failure-case-tiktoken-tangent). |
| 2026-08-13 | failure | A rule scoped to the wrong moment reads as coverage but catches nothing. "Verify source before critical edits" would have *passed* the real incident it was meant to prevent: an agent read one field of a payload, ignored two others sitting beside it, and reported "barely used, collapsed to roughly one global program". The source was verified — it was under-sampled, and a qualitative impression was then phrased as a measurement. | Audit a guardrail by replaying a concrete failure against its exact wording, not by matching its topic. Under-sampled evidence and unquantified quantifiers are distinct failure modes from fabrication, and no anti-fabrication rule covers them. |
| 2026-08-27 | failure | The stale-handoff signal excluded exactly one path, `.agent-context/HANDOFF.md`, so a turn that only wrote `PROGRESS.md` or `MEMORY.md` was told to go rewrite the handoff: bookkeeping triggering bookkeeping. Verified by running the hook after touching each file alone — all three fired. The 2026-08-03 lesson ("a signal that fires on almost every turn is measuring the wrong moment") had been applied to the signal's *severity* (advisory, not blocking) but never to its *trigger*. | When a lesson says a signal measures the wrong moment, change what it measures. Softening the response leaves the false positives in place. Exclusion is now the whole `.agent-context/` directory; the smoke tests in `COMMANDS.md` assert that a memory-only turn stays silent. |
| 2026-08-27 | lesson | The signal still measures "worktree dirty", not "handoff stale": it reads `git status --porcelain`, so a turn that commits its work silences the reminder even when the handoff is genuinely out of date. Known and accepted for now. | Do not read a green stop hook as evidence the handoff is current. If this matters later, compare the handoff against `git log -1` on the branch as well as the worktree. |
| 2026-08-27 | failure | A section that mixes an unconditional cut instruction with a soft add instruction loses the add. `Response Style` told the agent to "Remove pleasantries, repeated points… redundant summaries" and, two lines later, to order bullets "problem, impact, recommendation". A reply then listed 8 long-run risks with problem and impact, gave a verdict on only 3, left 5 with no action at all, and closed with a conditional "if you want to do more, then…" instead of a decision. The clause was present and lost. | Do not pair a mandatory deletion with an optional addition in the same section; the deletion always wins under context pressure. Make the addition the mandatory half, name what satisfies it (here: "safe to ignore, because …" counts), and state the closing step as a decision rather than a conditional list. |
| 2026-08-27 | lesson | A clause's presence in the rule is not evidence it works, and topic coverage is not evidence either. Per-clause replay against recorded incidents separated 5 hits (`Safety`) from 0 hits across 32.4% of the file, which no amount of reading the prose had revealed across five previous commits. | Before adding to an always-on rule, run the new wording against `MEMORY.md`'s recorded failures and state the hit count. Zero hits is not disqualifying on its own — a clause can still survive on "no invocation-free mechanism covers it" — but it must then say so explicitly instead of implying an incident it never would have caught. |
| 2026-08-03 | failure | Assumed Codex `PreCompact`/`SessionEnd` could drive a handoff write. Neither can: `PreCompact` ignores plain stdout and supports only common output fields, and `SessionEnd` is explicitly advisory. `Stop` is the only Codex event that can steer the agent after a turn. | Check an event's supported output fields before designing around it; "the event exists" does not mean it can influence the agent. |

## Open Questions
| Date | Question | Why it matters / how to settle it |
|------|----------|-----------------------------------|
| 2026-09-18 | Settled by CodeBuddy support (was 2026-09-03): the IDE loads flat plugin `rules/*.mdc` (`allPluginRules: Returning 1`, `[rule-context] allRules: 1`), but the rule-injection budget is **6000 chars and over-budget rules are silently omitted** — our 6741-char always rule was dropped in some conversations. Also settled: the IDE loads the plugin live from the repo source dir, so uncommitted edits affect new sessions immediately and the manifest version is cosmetic there. Still open: does IDE Stop send Claude's `last_assistant_message` or Cursor's `status`? | Discovered by contradiction: bundled-JS code reading twice pointed to wrong conclusions (a `.md`-only filter that is not the running path; a "no skills field → no discovery" reading), while live logs and support diagnosis settled both. Lessons: bundled code is not proof of the running path; a hard host limit can hide behind "loaded successfully" logs — registration ≠ injection. Enforced 2026-09-18: the validator now fails past 6000 whole-file characters (conservative: the vendor figure was not split into frontmatter/body) and warns past 5700 with the remaining margin, mutation-checked by five regressions. The coincidental 6000 body attention proxy from 2026-09-01 was removed so one number does not carry two meanings; the 4200 clause-audit line stays. Compress or move detail to a skill — raising the limit re-enables the silent drop. Stop-payload half: dirty a non-memory file in an IDE session and observe the hook payload. [Evidence](worklog/2026-09-18.md#codebuddy-plugin-rule-diagnosis). |
| 2026-08-27 | Does Codex's default prompt cover the response-order guidance that the 2026-08-27 audit deleted as host-redundant? Only Cursor was checked, from that session's own system prompt. | One deleted line rests on this alone: "Answer first, then explain. Default order: conclusion, key evidence, next step." (79 chars). Every other cut also had a replay reason or a reverse hit, so only this one would need restoring. Settle by reading Codex's default instructions, or by asking a Codex session a multi-finding question and seeing whether it leads with the conclusion. |
| 2026-08-05 | Do the two epistemic clauses ("record unverified entries as assumptions"; "quantified claims need a computed number") actually change behavior, or are they inert prose? Assumed effective, not measured. | They contradict the 2026-07-01 finding that prose conventions get ignored under context pressure while objective signals do not. Settle by reading `MEMORY.md` additions and asserted conclusions across the next several projects: if nothing is ever marked assumed or labeled an impression, the axis is decorative and needs a hook-side signal instead. |
