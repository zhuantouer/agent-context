# Project Progress

## Current Focus
CodeBuddy host adapter: same plugin package, third manifest, hybrid rules+Claude-hooks path.

## Completed
- [x] (2026-09-03) Adapted the plugin for CodeBuddy without a second protocol copy. Added `.codebuddy-plugin/plugin.json`, repo marketplace, `hooks/codebuddy-hooks.json`, and `.codebuddy/settings.json` so this repo dogfoods the plugin. CodeBuddy is a hybrid host: protocol via plugin `rules/` (like Cursor), hook I/O via Claude-style JSON (like Codex). SessionStart does not inject the protocol. Installer, validator, and hook tests cover the new host. Live CodeBuddy session still unverified.
- [x] (2026-09-01) Restored the missing Karpathy bug-verification loop from `multica-ai/andrej-karpathy-skills`: capture a minimal reproduction or failing check before a feasible/proportionate fix, rerun the same check afterward, and state substitute evidence when reproduction is unsafe or unavailable. Reorganized the overloaded `Safety` section into `Execution` (authorization, bug loop, scope, validation, secrets) and `Evidence` (source coverage, facts/assumptions); `Response Style` stayed intact because all three clauses govern output. Rule body 3841 → 4107, still below the 4200 review line.
- [x] (2026-09-01) Applied the eight-item capability review to the core rule. Evidence reads only relevant material that could change the conclusion; same-defect sweeps target likely locations proportionate to risk; confirmation is reserved for missing material user choices; validation scales to risk and reports unavailable checks; optional `ARCHITECTURE.md`/`COMMANDS.md` no longer act as mandatory dependencies. Memory now has a canonical owner but permits resume summaries, start-time bookkeeping triggers were removed, and next-step recommendations apply only to unresolved/actionable work. The validator's 6000 line is now a strong warning, not an error, and preventive clauses may address predictable failures rather than requiring a prior incident. Rule body 3666 → 3841, below the 4200 review line.
- [x] (2026-09-01) Audited what the old cap had cost in clarity and restored it (+100 chars, 3566 → 3666, still 534 below the review line). Four real losses: `Scale to the request` had lost its object and no longer named the `Architecture Checkpoint` it scopes; the confirmation exception had lost "explicitly", widening it to cover vague requests; `Startup` still said "run `bootstrap-context` first" and leaned on a clause two sections away for the qualifier, now "before substantive work"; `Response Style` was left unscoped on a coverage claim that held only for the next-step half, now "When you report findings, risks, or options". Also replaced the ambiguous "enumerate what the source actually contains" with wording that states the failure it replays. Re-checked the four earlier deletions against a no-budget standard — all still justified as duplication, none restored.
- [x] (2026-09-01) Turned the always-on rule budget from a hard cap into a two-tier review trigger after the user objected that it was constraining the product: warning past 4200 chars, error past 6000, both negative-tested (4616 → warning with exit 0, 6866 → error with exit 1). The old 3600 cap was justified on token cost, and measurement retired that premise — the body is ~890 tokens, 0.45% of a 200k window, 59% of the plugin's 6047-char per-session footprint. Rewrote the enforcing comment to name attention rather than cost, and anchored both tiers to project facts: 4200 is the size the 08-27 audit judged bloated, 6000 is where the rule alone would exceed everything else injected. Current body 3566, 634 chars below the review line.
- [x] (2026-09-01) Audited the always-applied rule for capability suppression (a different axis from the 2026-08-27 cost audit) and executed the result. Measured 28 of 33 clauses as unconditional, so the protocol applied full weight to factual questions; added a proportionality clause to the header and paid for it by deleting the universal `avoid cycles` prescription, a routing sentence that restated the Ownership table, `Files live in .agent-context/`, and a duplicated `Rewrite it`. Scoped the confirmation list with `the requested change itself needs no re-approval` (it previously asked permission for the thing just requested) and changed `Do not guess behavior` → `Do not assert unverified behavior` (the old wording banned hypothesis formation). Net −2 chars while adding a clause: 3568 → 3566/3600, headroom 34. First attempt came in at 3659 and the validator rejected it.
- [x] (2026-09-01) Fixed the surgical-edit clause's misplaced emphasis: `Mention issues with a different root cause` → `Report issues with a different root cause, with locations`. The old wording made disclosure the weak half of a sentence whose weight sat on the prohibition, and left a second reporting tier that could not be checked. Kept the "do not fix unless asked" half (recorded scope-creep rationale). Swept the repo: the pattern appeared nowhere else. +15 chars, 3568/3600 — only 32 chars of headroom remain, so the next clause has to displace something.
- [x] (2026-08-27) Closed a third review of the capsule work. The blocking finding was arithmetic I had skipped: capsule (1399) plus the full-file read my own wording invited (2936) came to 4335 against the old 4363, so the reported saving was ~28 chars, not 728 tokens. The capsule is now self-sufficient — clipped fields get `…` and no instruction, only never-carried sections earn a pointer, and `Notes for Next Agent` joined the carried fields (7 total). Deleted the unreachable drop-order in favour of caps that bound the total by construction; clipping now cuts on Chinese sentence ends. Collapsed four contradicting definitions of the handoff format to one owner. Added `scripts/test-hooks.py` (20 stdlib tests, ~1s) covering capsule selection, clipping, the size backstop, stop-signal conditions, and the template/capsule contract; it found two real bugs on its first run and each new invariant was negative-tested. Merged the duplicate `# Project Memory` document out of `MEMORY.md` and compacted this ledger. Measured: Cursor 1342 chars, Codex 4956, rule body 3553/3600.
- [x] (2026-09-02) Fixed a user-reported terminology defect: replies said "金标" where the recommender-systems field writes "ground truth". Cause was a literal `translate borrowed terminology into the current project's terms` added 2026-08-25 — still in `HEAD` (`4429f3f`), removed from the worktree by the uncommitted 08-27 compression, which left no counterweight since the correctness-preservation list omits terminology. `Response Style` now says to write each term the way its field does and to gloss rather than replace an unfamiliar one; a preservation-list entry was rejected because it cannot specify which form to keep, and field usage rather than language decides (召回率 vs "ground truth"). Swept `plugins/` and `README.md` for other clauses pushing vocabulary rewrites — none. Body 4239 → 4309, 21 tests pass, installed copy refreshed.
- [x] (2026-09-01) Audited the protocol on the cost axis after the user named the balance ("guide better work, but do not impose unnecessary work") and closed the asymmetry: nothing in the plugin measured imposed work, since both stop signals push toward more of it and the size lines measure resident context. The validator's clause-audit prompt now also asks whether each clause names a condition that turns it off on trivial work. A clause-by-clause pass found 12 of 16 clauses already carry an off-switch; the two exceptions were both introduced hours earlier and are now re-scoped — the bug loop applies to a behavior bug, and `Evidence` bounds reading to the sampled list or section instead of "every source you cite to its end", which had licensed reading a 3000-line file to cite one function. Direction check over all 47 recorded decisions: ~12 tightened, ~18 loosened, but 11 of the 18 landed in the last two days. Body 4204 → 4239.
- [x] (2026-09-01) Replaced four self-assessed clauses with checkable ones after the user approved all: the bug loop lost its double hedge and became unconditional with an explicit unsafe/impossible escape; `Evidence` swapped self-referential "all relevant evidence" for "read every source you cite to its end" + "search the full scope claimed"; `Ownership` swapped "likely to help future work" for "outlives this turn and its owner does not already carry" and now states every file is optional, resolving a contradiction with `Architecture Checkpoint` and `bootstrap-context`. Body 4109 → 4243 → 4204 after the review-line audit tightened two clauses; accepted 4 chars over rather than reword for 4 chars. Full-scope sweep confirms no stale copies of the retired phrasings.
- [x] (2026-09-01) Reviewed the Karpathy-loop / `Execution`+`Evidence` round and repaired the trigger drift it left: six stale restatements of the `HANDOFF.md`/`PROGRESS.md` write triggers in `handoff/SKILL.md` (description + body), `update-progress/SKILL.md` (3 lines), `COMMANDS.md`, and `README.md` — two carried the `touched files` field retired on 2026-08-27, i.e. the same defect class survived its own sweep. `handoff/SKILL.md` now defers to the protocol's `Ownership` table instead of keeping a competing list. Added `test_retired_handoff_field_stays_retired`, negative-tested both directions; its first version wrongly flagged the clause that forbids the field and its scope missed `README.md`, both fixed. 21 tests pass, validator clean, rule body 4109 chars, installed copy byte-identical.
- [x] (2026-08-27) Cut the plugin's runtime cost after a second reviewer's critique: session-start injection now builds a fixed-size resume capsule from six selected `HANDOFF.md` fields with per-field caps instead of slicing the whole file (Cursor 4363 → 1459 chars, Codex 7800 → 5061); the stale-handoff signal excludes all of `.agent-context/` so bookkeeping no longer triggers bookkeeping; hook messages carry state only, with the protocol restatements removed; `Ownership` writes and the defect-class sweep gained thresholds; `handoff` template 9 → 7 sections (touched files dropped, source freshness folded into validation); `bootstrap-context` no longer forces seven files on a project with nothing to record; `last verified` replaced by `verified against: <commit>` + covered paths. All six hook smoke tests pass, capsule branches negative-tested, rule body 3541/3600. Report: `~/.cursor/projects/Users-zishuaizhang-workspace-agent-context/canvases/plugin-runtime-cost-review.html`.
- [x] (2026-08-27) Audited all 20 behavioral clauses of the always-applied rule against an admission test (artifact/observable output; no invocation-free mechanism already covering it; replays against a recorded failure) and executed the result: rule body 4239 → 3495 chars, the first net decrease since `2f59826`. `Response Style` 982 → 450, `Architecture Checkpoint` 390 → 173, two hygiene bullets and the disproven "verify source before critical edits" clause deleted, `Safety` kept at full length on 5 replay hits and gaining the relocated epistemic line. Added a 3600-char budget for any `alwaysApply: true` rule, asserted in the validator and negative-tested. Report: `~/.cursor/projects/Users-zishuaizhang-workspace-agent-context/canvases/core-rule-admission-audit.html`.
- [x] (2026-08-27) Closed the `Safety` section's fix-scope blind spot: the surgical-edit clause now requires a same-defect sweep (sibling call sites, parallel modules, other UI surfaces, every host) before a fix counts as done, repairs of same-root-cause instances, and an explicit list-and-ask for the risky ones; "unrelated" is now defined by root cause. Rule body 3849 → 4242 chars. Validator passed; Cursor plugin reinstalled.
- [x] (2026-08-25) Added a short `Response Style` section to the canonical rule for Cursor and Codex: answer-first order, plain language, focused paragraphs, consistent findings, explicit uncertainty, and accuracy-preserving exceptions. Validator passed; Cursor plugin reinstalled.
- [x] (2026-08-13) Extended the core rule's `Safety` section to govern asserted conclusions, not just memory writes and pre-edit checks: enumerate the source's contents before concluding, and back a quantified claim with a computed number or mark it an impression. Triggered by a real incident the 2026-08-05 clause would have passed. Always-on cost +198 chars, rule body now 2865 chars (~50 added tokens by the chars/4 estimate; not tokenizer-measured).
- [x] (2026-08-05) Closed the protocol's epistemic gap after reviewing a shared third-party global prompt: the core rule now asks writers to record unverified entries as assumptions, `MEMORY.md` owns open questions, and the `handoff` skill migrates anything that outlives the task before its rewrite discards it. Also documented Claude Code / OpenCode / Pi adaptation sketches in the root README as a TODO inviting PRs.

## Older Milestones
- (2026-08-01 → 08-03) Dual-host packaging: surveyed Codex plugins/skills/hooks, merged the split Codex package into one dual-manifest package with a single shared hook implementation and validator drift assertions, then fixed the install — the plugin was discoverable but `not installed`, which the desktop UI hides. Stop signals became advisory on both hosts, and the stale-handoff signal was decoupled from the large-file signal.
- (2026-08-03) Naming and cost: kept `agent-context` after all eight candidates were taken and moved differentiation into manifests and taglines; renamed `.agent/` to `.agent-context/` across 91 references in 17 files; trimmed the always-applied rule ~790 → ~721 tokens by folding `Update Triggers` into the `Ownership` table.
- (2026-07-01) Modular-design guardrails: the core rule's Architecture Checkpoint, a module map in the ARCHITECTURE guidance, modular habits in CONVENTIONS, and a conservative large-file signal in the stop hook.
- (2026-06-26 → 06-27) Built the plugin: memory files, handoff, conventions, validation, hooks, and lessons; then condensed the core rule and skills, added observable success signals, and made handoff next actions verifiable.

## In Progress
- [ ] Live Codex session check: the plugin now reports `installed, enabled` and the cached hook copy passes the SessionStart smoke test, but nothing has confirmed Codex itself firing the hook — needs an app restart plus hook-trust approval
- [ ] Behavior checks stacked on one fresh Cursor session after a reload, all still unverified: (1) do safeguards scale without exhaustive reading/search? (2) does a reply attach a recommendation to every reported risk? (3) does a fix list likely same-root-cause occurrences without scanning irrelevant hosts? (4) is the injected block the ~1.3k capsule with no "read the full file" trailer?

## Backlog
- Decide whether to split `Architecture Checkpoint`, the defect-class sweep, `Response Style`, and the large-file signal into an optional quality layer, leaving the core as pure memory. Deferred 2026-08-27: needs a product-direction call, since a second Cursor rule is also always-applied and only saves tokens if it ships disabled
- Verify whether Codex's default prompt covers the deleted answer-first ordering; restore those 79 chars if it does not (see the 2026-08-27 open question in `MEMORY.md`)
- Consider whether the stale-handoff signal should also compare against the last commit, not only the dirty worktree — committing work currently silences it
- Host adapters (tracked in root README TODO): Claude Code, OpenCode, Pi — PRs welcome. CodeBuddy shipped 2026-09-03; confirm a live session before calling it done
- Rewrite the install sections of `README.md` and `plugins/agent-context/README.md` for both hosts; they still document Cursor only, which now contradicts the dual-host taglines above them
- Decide whether the hooks should detect a legacy `.agent/` directory left by the old naming and point the user at it, instead of silently treating the project as un-bootstrapped
- Optional: have `bootstrap-context` offer an `AGENTS.md` pointer for Codex users who do not trust plugin hooks
- `MEMORY.md` is 26KB / 60 rows after the duplicate document was merged out. Load-on-demand, so it costs nothing per session, but it is a heavy read; consider whether decisions older than the current direction can move to a rationale appendix

## Blockers
- None. The shell recovered and all previously blocked validation has run.

## Context Freshness
- Last sync: 2026-09-03
- Last sync detail: CodeBuddy adapter installed (`agent-context@agent-context-marketplace`). Validator passed, 32 hook tests passed, `codebuddy plugin validate` passed after `skills: ["./skills/"]`. Live session unverified.
- Source revision: working tree (uncommitted)
