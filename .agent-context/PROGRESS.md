# Project Progress

## Objective
Keep agent-assisted research and execution goal-directed across sessions. Success: understandable decisions, evidence-based conclusions and preserved work history without duplicate bookkeeping or routine full-history reads.

## Constraints
One current-state map; daily logs own detail. No separate handoff, automatic daily files or calendar-based resume. Preserve capability and decisive context. Do not auto-commit or refresh installed plugins; leave unrelated plugin caches untouched. Preferences: CONVENTIONS.md.

## Current State
2026-09-21: direction-change patch reviewed and fixed — goal model is now three partitions (`## Objective` active line only / `## Other Goals` for parallel-or-paused lines with resume condition / `## Closed Questions` for closed ones), the mandatory rewrite fires only when a new question becomes the working goal, and the rule body was trimmed 4272 → 4190 chars. `python3 scripts/test-hooks.py` 32/32 OK, validator clean with no size warning. [Review](worklog/2026-09-21.md#review-of-the-direction-change-objective-rewrite-patch), [Fixes](worklog/2026-09-21.md#fixes-applied-to-the-direction-change-patch)
2026-09-20: repository is one plugin package at `plugin/` with no marketplace index — `.cursor-plugin/`, `.codebuddy-plugin/`, `.agents/` and `.plugins-cache.json` deleted, README marketplace option removed, validator/tests/docs updated, all three hosts reinstalled. Tradeoff: publishing to a host marketplace needs those files back. [Flatten](worklog/2026-09-20.md#repository-flattened-to-one-plugin-package-marketplace-distribution-dropped)
2026-09-20: CodeBuddy was loading the rule from the repo source tree — the installer had registered the repository as a directory marketplace (referenced in place), and repo-root `.codebuddy/settings.json` did the same at project scope. Both redirected to the installed copy; the dogfooding settings file was deleted. IDE-side reload/verification still pending — no IDE evidence yet, only CLI/registry state. [CodeBuddy fix](worklog/2026-09-20.md#codebuddy-rules-loaded-from-the-source-tree)
2026-09-20: plugin renamed to `agentic-protocol` — id, package directory and the canonical rule file; `.agent-context/` and both skill ids keep their names, so no project records migrate. Rename committed as `0e8d8cc` and pushed. Version 2.0.1 is still uncommitted, but all three hosts now run it (reinstalled during the flatten). 29 tests OK, validator clean. [Rename](worklog/2026-09-20.md#plugin-renamed-to-agentic-protocol), [Version](worklog/2026-09-20.md#version-set-to-201), [Reinstall](worklog/2026-09-20.md#reinstalled-on-all-three-hosts)
2026-09-19 (still current): one hook — Codex SessionStart delivering the protocol — and two skills; no host injects work-record content, the agent reads `PROGRESS.md` itself. Chinese rule canonical at ~3.3k chars against the 6000-char host limit; no English fallback. [Slimming](worklog/2026-09-19.md#plugin-slimmed-to-one-hook-and-two-skills)

## Next Check
Direction-change patch is fixed and green; before it can take effect in sessions, run `./scripts/install-local.sh all` (hosts still run the pre-patch copy). Open decisions: whether to trim the rule below 4190 (further cuts would remove behaviour this patch exists to enforce), and migrating this repo's own free-form `## Objective` to the three-part template. Then the pre-existing items below.
Confirm whether 2.0.1 was intended as 0.2.1 — the previous release was 0.2.0, so the jump is a major-version claim nobody has justified. Marketplace id availability is moot: distribution was dropped on 2026-09-20, so publishing now needs the indexes restored first. All three hosts now run the 2.0.1 copy (reinstalled during the flatten). In CodeBuddy, `/reload-plugins` and confirm the rules panel shows the `~/.codebuddy` path, then probe a fresh chat without file reads for `# Agentic 方法论`: the budget check passing is necessary, not sufficient. Never raise the limit. Replay the Chinese rule — with the English fallback gone, any regression has to be fixed forward in the Chinese text.

## Milestones
- 2026-09-20 — Plugin renamed to `agentic-protocol` (id, package dir, marketplace ids, rule file) and reinstalled on all three hosts; record directory and skill names unchanged; committed `0e8d8cc`, pushed. CodeBuddy install redirected from the repo to a local marketplace serving `~/.codebuddy/plugins/`; project-scope dogfooding file deleted. Repository flattened to one `plugin/` package with no marketplace index (distribution deliberately dropped). IDE-side verification pending; behavior equivalence after the rename is unproven. [Rename](worklog/2026-09-20.md#plugin-renamed-to-agentic-protocol), [CodeBuddy fix](worklog/2026-09-20.md#codebuddy-rules-loaded-from-the-source-tree), [Flatten](worklog/2026-09-20.md#repository-flattened-to-one-plugin-package-marketplace-distribution-dropped)
- 2026-09-19 — Plugin slimmed to one hook and two skills, with the protocol rewritten as the Chinese `Agentic 方法论`; committed per feature and pushed. Behavior equivalence is still unproven. [Commits](worklog/2026-09-19.md#committed-and-pushed-in-per-feature-commits), [drift fixes](worklog/2026-09-19.md#documentation-drift-after-the-two-skill--no-stop-cut).
- 2026-09-17 — Proactive-efficiency guidance added; recovery/packaging fixes validated. Decision-only replay remains preliminary. [Guidance](worklog/2026-09-17.md#proactive-lossless-efficiency), [review fixes](worklog/2026-09-17.md#review-follow-up).
- 2026-09-16 — State map plus dated evidence; no separate handoff or timestamp-driven bookkeeping. [Details](worklog/2026-09-16.md#daily-log-split).
- 2026-09-03 — Shared plugin supports Cursor, Codex and CodeBuddy; live host acceptance remains separate. [Evidence](worklog/2026-09-03.md#migrated-records).
- 2026-08-27 → 09-10 — Bounded recovery, proportional safeguards and milestone traceability; later design supersedes old snapshot mechanics. [Recovery](worklog/2026-08-27.md#migrated-records), [traceability](worklog/2026-09-10.md#migrated-records).
- 2026-06-26 → 08-03 — Plugin foundation, multi-host packaging and namespaced project memory. [Original date-range records](worklog/2026-09-16.md#imported-date-ranges).

## Deferred
Revisit host extensions, optional quality layers and historical-memory compaction only for a concrete need or measured failure. [Preserved opportunities and conditions](worklog/2026-09-16.md#deferred-opportunities).
- Renaming the plugin `agent-context`: rejected on 2026-08-03 (all eight candidates taken by competitors), **done on 2026-09-20** as `agentic-protocol` — plugin id, package directory, marketplace ids and rule file changed; `.agent-context/` and both skill ids keep their names. Hosts reinstalled. Marketplace id availability is **moot since 2026-09-20**: distribution was dropped, so publishing needs the three indexes and the `plugins/<name>` nesting restored first; the `homepage` owner mismatch (`thanezh` vs `zhuantouer`) likewise only matters if publishing returns. Version numbering is a separate open item, see Next Check. [Rename](worklog/2026-09-20.md#plugin-renamed-to-agentic-protocol), [Flatten](worklog/2026-09-20.md#repository-flattened-to-one-plugin-package-marketplace-distribution-dropped)
- Flattening `plugin/` to the repository root: **deferred, condition known**. A 2026-09-20 survey of superpowers / Claude Code / Cursor / agent-plugins.org found every install path (marketplace `source: "./"`, git-URL installs) resolves the plugin from the repo root, so nesting blocks publishing — but distribution was dropped the same day, so the benefit is currently zero and the cost is an include/exclude list for the copy installer. Revisit only if marketplace or git-URL distribution is wanted again. No-regret moves identified but not done: root Agent-Plugins `plugin.json`, per-harness exclusion list, version-bump script, written porting SOP. [Survey](worklog/2026-09-20.md#layout-survey-how-popular-multi-harness-plugins-organize-their-repos)
- Constraint check: the old `Current State` 800 / `Next Check` 400 caps died with plan A — `session-context.py` no longer reads `PROGRESS.md` at all (it injects the protocol for Codex only). Keep the map short because the rule asks for it, not because anything truncates it.
