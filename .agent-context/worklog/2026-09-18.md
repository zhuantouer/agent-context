# Work Log — 2026-09-18

## CodeBuddy plugin rule diagnosis

Purpose: explain why a CodeBuddy agent reported never having the plugin protocol and worked by inferring structure from `.agent-context/` files, while the IDE Rules panel showed the plugin rule registered and enabled.

- Corrected a 2026-09-17 misdiagnosis: bundled `allPluginRules()` code containing a `.md`-only filter was treated as proof that CodeBuddy ignores plugin `.mdc` rules. Live host logs falsify this. At 2026-09-18 10:48/10:50 the IDE logged `Adding rule for plugin agent-context … rules/agent-context-core.mdc`, `allPluginRules: Returning 1 plugin rule(s)`, `Successfully loaded plugin rule: agent-context/agent-context-core`, `[rule-context] allRules: 1; commonItems: 1`, and `[AgentContextBuilder] Reported plugin_capability_call for rule …`. Bundled JS is not evidence of the running path; UI state plus runtime logs outrank it.
- The IDE loads the plugin live from the repository source directory: the marketplace entry's `installedPath` resolves to `plugins/agent-context` in this repo, not `~/.codebuddy/plugins/agent-context`. Consequences: repo edits reach new IDE sessions immediately; the manifest `version` does not gate IDE freshness; the `~/.codebuddy/plugins/` copy is not the effective path for IDE chats.
- Timeline of the reported failure: the affected conversation's first stored message is 2026-09-17 16:29; the reinstall/marketplace activation ran at 17:54. Official Rules docs state rules are added only at session start and a new conversation is required after rule changes. Stored history for that session contains the protocol text only as line-numbered tool-read results (e.g. `6:# Agent Context Protocol` in an assistant message's tool-result payload), not as session-opening injection — consistent with the rule being unavailable when that session started. The SessionStart hook itself works: the work-record capsule appears in session context, and a manual hook run emitted the expected envelope without protocol (by design for CodeBuddy).
- Pending verification superseded the same day: CodeBuddy customer support confirmed the rule IS loaded and injected, but the host's rule-injection budget is 6000 characters and silently omits over-budget rules — our rule at ~6743 chars was dropped in some conversations. This falsifies the session-timing explanation above as the root cause of the protocol's absence (stale sessions still cannot gain rules mid-flight, but the budget explains it fully). It also retroactively gives the project's own 6000-char review line — set on 2026-09-01 as an advisory attention proxy — a hard host-side counterpart. The injection probe is moot while the rule exceeds 6000 chars; re-probe only after a fix brings it under budget.
- Validation: log lines quoted from `~/Library/Application Support/CodeBuddyExtension/Logs/CodeBuddyIDE/2026-09-18/agent-context__bbc2e084847f7f6bc767897f264d96cc.log`; history analysis counted marker occurrences and JSON field paths only, without exposing conversation content. No code, prompt, or plugin changes were made in this diagnosis.

## CodeBuddy skills not discovered

Purpose: the three plugin skills stopped appearing in the CodeBuddy UI; find the host-side cause rather than guessing.

- Ground truth from host logs: `[allPluginSkills] Plugin agent-context: 0 skill(s), installedPath=/Users/zishuaizhang/workspace/agent-context/plugins/agent-context` (repeated 11:01–11:06 on 2026-09-18), while pdf/playwright/agent-browser reported 1–3 skill(s). So the plugin, skills directory and SKILL.md frontmatter are all fine; discovery failed.
- The `skills` field has been `["./skills/"]` since the manifest was created (`9ac6303`), so this is not a regression from the recent compression or review edits. Likely a host-side behavior change, since the IDE bundle is dated 2026-09-05 and the host updates independently.
- A directory value is not scanned the way this layout needs: each skill lives in `skills/<name>/SKILL.md`, so a directory entry has no top-level `.md` child and yields 0. Changed the CodeBuddy manifest to declare the three SKILL.md files explicitly. An intermediate attempt that omitted the field entirely was not validated by any post-change host scan and was replaced; convention-based discovery remains unproven here (the pdf comparison was unreliable: it reported 3 skill(s) with a single SKILL.md on disk).
- Added a validator invariant: a declared CodeBuddy `skills` entry must name a `SKILL.md` file; a bare directory is now an error. Negative-tested with a temp copy containing `["./skills/"]` (exit 1) while the real tree passes.
- Verification is still pending and host scans on 2026-09-18 were triggered by chat activity, not by CLI installs: after the change the last logged scan was 11:06, before the 11:11 and 11:13 installs. A new scan (any IDE message, or `/reload-plugins`) is required before reading the count; do not claim success from the file change alone.
- Confirmed by the user after a full IDE restart (2026-09-18 ~11:19): the three skills are visible again in the CodeBuddy UI. Attribution is not isolated — the declaration change and the restart happened together, and no scan between 11:06 and the restart logged a non-zero count, so "declaring SKILL.md files" is the supported form enforced by the validator rather than a proven sole cause. The last pre-change scans reported 0 across several refreshes, so a stale-cache explanation alone is unlikely. Reverting to a directory declaration just to A/B it would break the UI again and was not done.
- Open detail: the same-day log shows three `Loaded plugin skill:` entries with an empty name, which likely correspond to these three skills. If the UI ever lists them without names, the host is deriving the name from the declared file path instead of the SKILL.md `name` field, and the fix would be to let it discover the skill directories instead. Resolved 2026-09-18: user screenshots show all three skills with correct names and the current descriptions, and the rule registered as always-on and enabled — the empty-name log field is host-internal metadata only, and the UI reads SKILL.md frontmatter. The remaining open item is whether an always-on plugin rule's text enters a NEW session's model context (fresh-chat probe, no file reads).

## General task efficiency

Purpose: the user asked to generalize execution efficiency beyond the cache/lock/wait incident and merge it into `Execution`. Optimize the total cost of finishing tasks and iterating, considering repeated use as well as the current wait.

- Continued the preceding uncommitted MDC compression: committed `cffb10a` has 6741 body characters; the intermediate compact draft had 5846 (5936 including frontmatter). It was only a text/size check, not a verified live-injection fix. One delegated semantic review lacked access to the baseline; another was aborted. Neither is counted as independent sign-off.
- Removed the separate `Execution Efficiency` heading and placed the guidance under `Execution`. Proactively seek efficient algorithms, implementations and workflows throughout tasks, especially at bottlenecks, repetition and waits. Use available evidence and likely costs; do not run a project-wide optimization exercise merely because something is waiting.
- Compare realistic cumulative time/resource savings, including reuse, with investigation, coding/change scope, verification difficulty/cost, risk, maintenance and restart/lost work. Choose optimization now, deferral or finishing/waiting unchanged. Repeated large savings can justify a costlier improvement; a short one-off wait may be the better choice. Realistic reuse is a premise to establish, not invented future workload.
- Action remains current-task, worthwhile, authorized, reversible and low-risk; otherwise disclose the candidate and missing evidence/choice. Preserve outputs, required side effects, data/validation coverage and reproducibility; check cache/shared-state validity where reuse/concurrency is involved and bound resources. No job interruption or exceeding authorized resources for speed without approval. Require equivalence plus comparable measured costs before lossless speedup claims; retain uncertainty. No supported net benefit means continue normally, not repeated optimization or polling.
- Added three manual acceptance cases to the plugin README, not a new skill or more always-on examples: a frequently reused algorithm where 20 confirmed runs could save 9 hours versus 4 hours coding/validation; the same change with one run or excessive numerical-validation cost; repeated workflow preparation with uncertain reuse validity. These numbers are synthetic estimates. No replay was executed in this round.
- Scope: the MDC, relevant README acceptance scenarios and durable efficiency preference; earlier manifest/validator/diagnosis edits remain untouched. No installation or commit. The rule stays a single always-applied source; no duty moved to an unloaded skill.
- Validation: 63 regressions passed; structure validation and `git diff --check` passed; no lint diagnostics. An initial temporary check used lowercase `proactively` against capitalized `Proactively`; corrected the check, not the rule. Final rule body 5893 characters, complete file 5983 characters/UTF-16 units and 5993 UTF-8 bytes. Versus `cffb10a`, body reduction is 848 characters (12.6%). Body/full-file headroom against the support-reported 6000 is 107/17 characters: within that figure, but narrow, not a guarantee about aggregate rule budgets or host wrappers. No permanent host-budget validator was added in this wording task.

Conclusion: generalized the decision criterion from local wait avoidance to evidence-based total task/iteration cost while preserving safeguards. Text/contract checks do not prove LLM behavior, actual speedups or successful new-session injection. The support report establishes a budget-related omission, not successful delivery in affected conversations; rule registration and old-session timing alone did not establish the cause.

## Host budget enforcement and wording repair

Purpose: a review found that the 6000-character host limit was still only advisory, was measured against the wrong quantity, and that the shipped rule had 17 characters of margin — so the silent-drop failure could recur unblocked. Also fixed three compression artifacts.

- Ordering mattered: the wording repairs add characters, and at 5983/6000 whole-file they would not fit. Compressed equivalent redundancy in the same pass (`time/resource savings` → `savings`, `coding/change scope` → `change scope`, `bind` → `:`, `If repro is unsafe/impossible` → `If unsafe/impossible`, `Search the claimed scope to support claims` → `Search the full scope a claim covers`, and similar). Rule is now 5923 whole-file characters, 5833 body — smaller than before the repairs, with 77 characters of margin.
- Wording repairs, each restoring lost operability rather than shortening further: `Inspect likely costs from evidence` → `Inspect likely bottlenecks using available plans/logs/timings` (the compressed form dropped what to inspect and where); bare `running alone doesn't justify waiting` → quoted `"Still running" alone does not justify waiting` (the quotation marks the agent's own claim, and the contraction was inconsistent with the file's `do not` style); `interrupting jobs` → `interrupting active jobs` (unqualified, it would require approval to stop a throwaway process the agent just started). Also `aren't`/`Excerpts aren't` → `are not`.
- Budget check is now a build failure, not a warning, and measures the WHOLE file. The support report did not break the figure into frontmatter versus body, and only the file figure cannot understate cost; if the real budget excludes frontmatter this is conservative by ~90 characters, whereas the opposite error is silent. Added a 5700 margin warning that prints the remaining characters, because a bare limit is a cliff one line can cross.
- Removed the old 6000 body review line: it shared the value with the host limit for unrelated reasons (an attention proxy set 2026-09-01), and one number carrying two meanings invites raising the wrong one. The 4200 clause-audit warning stays, so both current warnings are true signals: 77 characters from the hard limit, and body past the audit line.
- Five regressions added: over-limit fails with `host injection limit`, exactly 6000 passes, 5900 warns without failing, an on-demand (`alwaysApply: false`) rule of 9000 characters is unaffected, and the shipped rule stays within the limit. Mutation-checked the decisive one by downgrading the error to a warning: the test failed as required, then the script was restored.
- Validation: 68 tests pass (was 63), structure validation passes with the two intended warnings, `git diff --check` clean, no lint diagnostics.

Limits: 6000 is a vendor-reported figure, not one this project measured; aggregate budgets across multiple rules and host wrapper overhead remain unknown, so margin is not proof of delivery. Whether the rule text now actually reaches a new session's context is still unverified — that needs the fresh-chat probe after an authorized reload. Text checks do not establish agent behavior.

## Second compression pass

Purpose: reduce always-on rule text further on request, restricted to duplicate and inferable wording — no clause, condition, exception or format contract removed. 5923 → 5833 whole-file characters (5743 body), margin 77 → 167.

Removed as genuinely redundant: `bottlenecks` stated twice in one paragraph; `relevant log links` → `relevant links` (same row already says "not a log index"); `report all relevant occurrences` → `report all occurrences` (the clause already scopes to likely shared causes); `if present and current` → `if current` (an absent file cannot be consulted); `Log evidence before map conclusions/links` → `Log before map updates` (map updates already covers links); `no repeated state snapshots` → `no state snapshots` (stricter in the intended direction); merged the header and Startup sentences.

Reverted mid-pass after self-review: dropping `before continuing` from the value/cost reassessment saved 19 characters but turned a pre-continuation gate into possible hindsight reflection. Restored as `Before continuing, reassess value/cost if …`. Timing is a condition, not filler; the 19 characters are not worth it.

Deliberately not compressed: the whole safety paragraph (equivalence, before/after measurement, active-job approval, resource bounds, "not lossless"), `Stop at sufficient evidence`, `Excerpts are not full evidence`, `Failed experiments can teach`, `safe-to-ignore` (vs a bare "ignore"), and the `Milestones`/`Current State` heading vocabulary the hook selector depends on.

Validation: 68 tests pass, structure validation passes with the two intended warnings (167 below the limit; body past the 4200 audit line), `git diff --check` clean. Spot-checked that decisive clause strings survive. Further material reduction would require moving whole clauses into skills — a layering decision with behavior risk, not a wording change, and still unmeasured.

## Chinese rule translation and capability review

Purpose: user requested a fluent Chinese version of the canonical rule (expert C translated; expert A reviewed for goal alignment, further precision, and whether clauses could degrade agent capability).

- Three drafts landed in `rules/agent-context-core.zh.mdc` (not replacing the English rule): full translation → EN headings/frontmatter + mixed CN/EN terminology → user-style compression. Final: 2917 whole-file chars (1646 hanzi) vs 5778 EN; validator passes with no zh warnings.
- Goal-alignment verdict: description and body match the meta-capability objective (better/more-efficient/higher-quality work). The header proportionality clause ("questions need answers, not checkpoints/memory writes/invented next steps") is itself the mechanism that keeps the protocol from taxing trivial work. All heavy clauses retain their off-switches in Chinese ("只在…时", "按风险成比例", "若不安全或不可行", "被要求时才动手") — checked clause-by-clause against EN. The three remaining unconditional clauses (no secrets, no invented commands/percentages, proactive efficiency seeking) are cheap to comply with or deliberately unconditioned per the 2026-09-17 decision.
- Blocking conflicts found: (1) `CONVENTIONS.md` records "keep agent-facing rules in concise English; localized docs outside the protocol" — shipping zh as the rule requires explicitly overturning that decision; (2) zh carries `alwaysApply: true` alongside EN, so coexistence aggregates ~8.7k chars over the 6000 budget and would silently drop both rules on any reload — zh must replace, never coexist, and no reload may happen while both exist.
- Recommendation: stop compressing zh (3083 chars of headroom make further cuts worthless; the EN-side compression lesson about degrading qualifiers does not apply to a file with slack). Optional precision fixes: "证据足够就停" → "证据足以决策时就停" (sufficient-for-decision, an EN ambiguity carried over, not a translation defect); "跑完或等完" → "做完或等完".
- Behavior of the zh rule is unverified: zero replays run; text fidelity was verified by clause-by-clause comparison only. A token-cost comparison was proposed and dropped as not the point per the user.

Conclusion: the protocol as written is a failure-mode filter with off-switches, not a capability limiter; the zh translation preserves that property. The open risks are structural (convention conflict, coexistence double-drop, no behavior replay), not textual.

## SessionStart content injection removed (plan A)

Purpose: user chose plan A — delete the SessionStart injection of `PROGRESS.md` content, keep protocol injection for Codex only.

- Rationale: an injected excerpt is a second source of truth. It can silently omit a constraint while looking authoritative, and the "over-budget sections are omitted with source labels" safeguard existed only to mitigate injection itself. Removing injection deletes the failure class.
- Code: removed `build_capsule`, `clip_field`, the field caps and the section-selection constants from `hooks/scripts/session-context.py` (~190 lines); the file now builds the Codex protocol envelope only. Removed the `sessionStart` entries from `hooks/hooks.json` (Cursor) and `hooks/codebuddy-hooks.json`; `hooks/codex-hooks.json` is unchanged because Codex has no `rules/` slot.
- Tests: deleted the capsule/clipping/omission cases from `scripts/test-hooks.py` (~350 lines) and replaced them with host-envelope assertions — Codex gets protocol text, Cursor/CodeBuddy register no session hook. 43 tests pass; `node scripts/validate-template.mjs` clean apart from the intended headroom warning.
- Rule: `## Startup` now states the mechanism ("this protocol is injected every turn; `.agent-context/` is not, so read it yourself"), which removes the dangling reference the injection used to provide. EN rule compressed 6865 → 5977 to get under the 6000 host limit — it was already over budget at HEAD, so this was prerequisite work, not scope creep.
- Docs: root `README.md` (pain-point table, work-record paragraph, hooks section, host TODO table, invariants), plugin `README.md` (acceptance item, test-coverage line), `scripts/install-local.sh` hint, and this project's `ARCHITECTURE.md` / `COMMANDS.md` / `PROGRESS.md` / `MEMORY.md`.

## Protocol self-failure case: tiktoken tangent

Purpose: the reviewing agent (running under this very protocol) derailed from a capability review into installing tiktoken; user asked why, and the post-mortem itself is protocol-behavior evidence.

- Chain: turn 1 correctly gathered review evidence, but its closing "recommended next step" included an unverified-premise token measurement ("use the existing build script" — no such script exists). When the user said "继续", the agent expanded that word to include executing its own recommended next step, then escalated from a failed cheap import to `pip install --user` for a question contributing ~zero decision evidence to the user's actual question (do clauses restrict capability).
- Three in-rule interceptors were present and did not fire: "Stop at sufficient evidence" (all review evidence already in hand), "reassess value/cost when effort grows materially" (the import→install escalation), and "report checks that cannot run" (the correct default after the first failure).
- Verdict: primarily an execution failure, not a rule-coverage failure — direct evidence for the 2026-08-05 open question on whether epistemic clauses are inert under context pressure. Two genuine blind spots found: (1) "state unknowns" labels generate TODO gravity — nothing says an unknown is a boundary declaration, not a backlog item; (2) the header's "invented next steps" clause governs answers to the user, not the agent's own recommended next steps becoming an execution queue on a later "继续".
- User interrupted the install; no environment change completed.

## Chinese rule becomes canonical

Purpose: adopt the Chinese rule as the single protocol file, after the user reframed the rule as a meta-capability prompt (a working method / SOP) rather than plugin documentation, and after confirming Codex still needs SessionStart injection.

- `rules/agent-context-core.mdc` now carries the Chinese protocol: **3018 chars vs 5977 for the English version (‑50%)**, leaving 2982 chars of headroom against the host's 6000-char injection budget. The English version is preserved at `rules/agent-context-core.en.mdc.bak` (uncommitted; ends in `.bak` so no host loads it and the validator ignores it). The parallel `agent-context-core.zh.mdc` was deleted — two `alwaysApply: true` files in `rules/` would aggregate ~8.7k and drop both silently.
- Header reworked into the meta-capability frame: `description` is now `Agent 工作方法论：目标对齐、证据纪律、跨会话记忆`, and line 8 changed from descriptive ("本协议用于…") to imperative ("按本协议工作：…"). The proportionality clause keeps its scope label ("以下规则按任务规模启用"), which restores the `Scale to the request:` label that had been compressed out of the English worktree copy and is still present in `git show HEAD`.
- Codex check (user asked to verify against current docs): official `developers.openai.com/codex/rules` and `learn.chatgpt.com/docs/agent-configuration/rules` both define Codex **Rules as sandbox exec policy** — Starlark `prefix_rule(pattern=…, decision=allow|prompt|forbidden)` under `~/.codex/rules/`, not prompt injection. Codex's only always-on instruction channel is `AGENTS.md` (layered, 32 KiB cap) plus hooks. So `INJECT_PROTOCOL_HOSTS = {"codex"}` stays; the 2026-09-03 "Codex has no rules slot" decision still holds. Trap recorded: `rules/` means `.rules` exec policy on Codex but `.mdc` prompts on Cursor/CodeBuddy — same name, different semantics.
- Test contract updated for the single-file architecture: three tests iterated `(RULE, RULE.with_suffix(".zh.mdc"))`. Two became single-file; `test_translated_rule_keeps_the_same_sections` no longer has a translation to compare, so it became `test_canonical_rule_keeps_the_expected_sections`, asserting the eight section headings directly (keeps the regression value without a second file).
- `CONVENTIONS.md` line 7 said agent-facing rules must be English; rewritten to state skills stay English while the always-applied rule is Chinese with English domain terms. Left alone: `update-progress/SKILL.md`'s "keep English headings" line, which is about `PROGRESS.md` section names the hook selector depends on.

Validation: `node scripts/validate-template.mjs` passes with **no warnings at all** (the two size warnings are gone; body is now under the 4200 audit line). `python3 scripts/test-hooks.py` 43 tests OK after the contract update. Grep confirms no remaining `zh.mdc` references in `scripts/` or `plugins/`.

Unverified: no behavior replay of the Chinese rule; the 6000-char figure is vendor-reported and its frontmatter/body split is unknown, so headroom is not proof of delivery. Prefix-cache benefit from dropping PROGRESS injection remains an untested assumption (host injection order is not exposed).

## Rule rewritten as an executable working method

Purpose: the user reframed the always-applied rule as a meta-capability prompt — how the agent should work in any project — rather than plugin documentation. Fourteen review rounds followed. Recorded because most are wording choices a later session would plausibly revert without the reason.

- **Goal Alignment rebuilt as a method.** Extract the result, acceptance criteria and output shape; infer criteria/shape and mark them assumption, but **ask when the result itself is unclear** (a wrong goal costs more than one question). The user's stated method is not the goal. The user may not know the optimal method, so look for a better one: implementation-level swaps are authorized, anything touching scope/architecture/irreversibility still goes through the ask list. Plans stop at the next decision point. The anti-tangent check became a **pre-step question** (`它的结果会改变哪个决策？`) instead of the old `reassess before continuing`, because [the tiktoken tangent](#protocol-self-failure-case-tiktoken-tangent) was caught by the old wording only in theory — it fires after the investment rather than before it.
- **Wording decisions, with the reason each survives review:** `影响重大` not `后果重大` (consequential is neutral; 后果 reads as "something broke"); `对达成目标的作用` not `贡献`, and `失败的实验也能提供信息` not `有价值` (both originals are laudatory, a tradeoff needs neutral); `实质性改动` not `非琐碎改动` (translationease, and it matches Startup's `实质性工作`); `behavior bug` left in English with no gloss (the rule keeps ~20 English terms and `行为类` was the odd one out — that translation was the source of the confusion); `按任务规模启用` kept as an explicit scope label (the English worktree copy had already lost `Scale to the request:` once, see [the canonical entry](#chinese-rule-becomes-canonical)); the Redis example kept concrete at the user's request, on the reasoning that anchoring beats generalizing.
- **Execution:** `本次改动内的合并、精简与清理` added as a traceable change source — the `本次改动内` boundary is load-bearing, since without it the clause licenses arbitrary refactoring, exactly the scope creep the 2026-08-27 decision guards. Verification was split from the honesty clause, given a label (`验证：`) and an explicit two-way reading (`低风险别过度，高风险别省`); the original was repeatedly read as guarding only against over-validation.
- Character cost 3018 → **3367** (+349 across 14 rounds), still zero warnings and 2633 of headroom. Every one of these edits would have been a tradeoff against the limit in English; the Chinese switch is what made precision free.
- Validation: `node scripts/validate-template.mjs` passes with no warnings; `python3 scripts/test-hooks.py` 43 tests OK after the single-file contract change recorded in [Chinese rule becomes canonical](#chinese-rule-becomes-canonical).

Conclusion: clauses now state actions with checkable bounds rather than principles. No replay has been run — text and contract checks say nothing about the Chinese rule's behavior.

## Rule reorganized around the workflow

Purpose: the per-line review had finished, but the user asked whether the sections themselves were grouped and ordered sensibly — same-kind rules together, in an order that matches how the agent actually works.

- Sections 8 → 7. `Architecture Checkpoint` (a single line) folded into `Execution`, where it joins the other "before doing X" clauses; efficiency (four paragraphs) left `Execution` for its own `Efficiency` section; `Ownership` and `Recordkeeping` merged into `Project Memory`. New order: Startup → Goal Alignment → Execution → Efficiency → Evidence → Response Style → Project Memory — start, aim, act, speed up, justify, reply, record. The user chose both: efficiency as its own section, memory last.
- The substantive gain is that project memory stopped being split three ways with two parts at opposite ends of the file (read in Startup, organising principles in Ownership, write in Recordkeeping). File table, ownership self-check and write triggers now sit together, and at the end — which is also when the writing happens.
- One clause was split rather than moved: `出现有意义结果…对比预期证据与实际证据…` stays in Goal Alignment, while `写 log / 更新 map / 暂停前只保存尚未记录的重要状态` moved to Project Memory and merged into the existing "先写 log，再更新 map" line instead of being duplicated beside it.
- Verification: compared the pre-reorganization text clause by clause — 93 clauses, 92 carried verbatim; the single mismatch is the intentional split above, whose two halves were each confirmed present in their new section. All seven ownership-table rows intact. 3311 chars (‑35, one fewer heading line), zero warnings, 43 tests OK.
- Two test references had to follow the rename: the section-set assertion (8 → 7 names) and `test_protocol_body_excludes_frontmatter`, which used the literal string `## Ownership` as its "body was extracted" marker. The second was missed on the first pass — the earlier sweep only caught `RULE.with_suffix(".zh.mdc")` call sites — and it was the failing test that surfaced it, i.e. the section regression guard earned its keep immediately.

Conclusion: grouping and order are now deliberate rather than inherited from the English file's compression history. Nothing was lost, but this remains text-level verification — no replay has been run.

## Unverified

- Whether an always-on plugin rule's text enters a new CodeBuddy session's model context (probe above). Plan A makes the probe the only remaining check: with no content injection, protocol presence is the sole thing to verify.
- Whether "the agent reads `PROGRESS.md` itself" fires reliably on Codex, where the protocol arrives as hook output rather than a rule file. Replay pending.
- Whether the IDE Stop event sends Claude's `last_assistant_message` or Cursor's `status` to the stop hook (unchanged from 2026-09-03).
- Chinese-rule behavior: no replay executed; equivalence in agent judgment is untested. Do not reload plugins while zh and EN both carry `alwaysApply: true`.
