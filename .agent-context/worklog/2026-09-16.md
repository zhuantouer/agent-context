# Work Log — 2026-09-16

## Daily log split

Purpose: keep the current-state map small as history grows, without daily handoff rituals.
- Changed the protocol and update-progress templates to keep goal/now, coarse milestones and relevant links in PROGRESS; details go to dated logs by task. The recovery hook does not scan or read logs, compute yesterday, or create today's file.
- Validation: `python3 scripts/test-hooks.py -q` — 53 passed, covering overnight/weekend/long-gap recovery, interleaved task links, no log reads/writes, daily-log-only Stop silence and old inline-history/cached-entry compatibility. Milestones were first misclassified as unknown sections, then the same check passed after the minimal classification change. Structure validation, Shell syntax and diff checks passed; no lint diagnostics.
- Migration: all 26 existing work/milestone entries and 7 deferred entries verified verbatim and exactly once before source removal, in 11 dated files. Two entries carry date ranges, retained in the explicitly marked import section below rather than assigned invented occurrence dates. All 23 committed historical entries were rechecked after migration; all 7 map links resolve to existing headings.
- Size evidence: PROGRESS before migration was 19193 characters; the initial compact map measured 2233 before final status edits. Rule body is 6257 characters (5966 before this turn), so no claim that the always-on prompt became smaller. The size warning is accepted for explicit ownership and recovery boundaries; detailed formats and migration rules remain on demand in the skill.
- Conclusion: routine state-file reads no longer include detailed history. End-to-end token savings and live-host behavior remain unmeasured. No commit or installed-plugin refresh; next validation is the cross-session replay, not another planning layer.

## Retired dev-repo protocol pointer

Purpose: remove a root pointer that duplicated the plugin's own injection path.
- Deleted the repository-root `AGENTS.md`, added 2026-08-03 and last edited 2026-09-04. It only pointed at `plugins/agent-context/rules/agent-context-core.mdc` and stated that this repository develops the plugin and uses it on itself, so it constrained this repository rather than shipping the capability to other projects.
- Why the earlier "keep" advice was wrong: it treated a development-repository pointer as if it were the integration point for projects using the plugin. Non-plugin hosts need an entry point inside the target project, not in this repository; duplicating one here invites drift.
- Also changed `bootstrap-context` to read host instruction files but not create or overwrite them to install the protocol. Confirmed no other tracked file referenced `AGENTS.md`.
- Validation: `python3 scripts/test-hooks.py -q` — 53 passed; `node scripts/validate-template.mjs` passed with the expected size warning. `git diff --check` clean; not committed, not installed.
- Known effect: an agent maintaining this repository without loading the plugin no longer receives the protocol automatically. Live behavior and token effects remain unmeasured.

## Deferred opportunities

Imported from PROGRESS.md on 2026-09-16; conditions below are not an automatic task queue.
- Live host acceptance: after authorized refresh/reload, run the current multi-turn replay and check protocol loading, bounded recovery, useful recommendations and restraint. Historical Codex/CodeBuddy installation checks did not establish live behavior; old capsule-size expectations are superseded.
- Optional quality layer: revisit only if behavior/cost measurements show the current safeguards impose unnecessary work; do not split merely to move prose.
- Response ordering on Codex: revisit if live output lacks usable answer-first recommendations; see MEMORY.md.
- Additional host adapters (Claude Code, OpenCode, Pi): revisit with a concrete host request and acceptance environment, not as an automatic extension of this refactor.
- Legacy `.agent/` naming detection or an AGENTS.md fallback for untrusted hooks: revisit when an actual project needs it.
- Compact historical MEMORY.md rationale when its read cost impedes a concrete task; retain decisions needed to explain current boundaries.
- The former commit-vs-handoff staleness issue is retired with the reminder, not a pending feature.

## Migrated records

Imported verbatim from PROGRESS.md on 2026-09-16. These are historical reports, not current instructions or newly verified outcomes. Original dates and qualifications are retained.

- 2026-09-16 — Fixed cached Stop entry compatibility. User's old Python path reproduced exit 2 / file not found; the removed Cursor Shell entry shared the defect. Added forwarding-only entries to current implementations, preserving stdin/host/exit behavior without reviving handoff writes. New tests first failed, then full suite passed (45 tests); the reported old command now exits 0 with {}. Structure, Shell syntax and diff checks passed. Host cache refresh itself was not performed.
- 2026-09-16 — Single-record refactor: 42 regression tests passed; structure, shell syntax and diff checks passed; no lint diagnostics. Restores PROGRESS-only projects, ignores stale handoff after consolidation, omits long sections rather than altering constraints, ignores fenced history headings, and bounds unstructured legacy input. These failures were reproduced before correction. Retired the handoff skill/file and mtime reminder; three skills remain, all host manifests now 0.2.0. Preserved all 23 pre-existing completed/milestone entries verbatim and consolidated useful handoff state. Rule body 5966 characters (5821 before this refactor), advisory size warning accepted; no net token saving claimed. Scoped read-only review found no must-fix issue. Not committed, installed or behavior-tested in a fresh host session.
- 2026-09-16 — Prior goal-alignment iteration: protocol and four skills gained expectations, bounded exploration and outcome gaps; 38 tests passed, structure/shell/diff checks passed. Rule body 5821 characters; worst capsule 2155/2200, Codex message below 10000. Not installed or committed. Dual-record design superseded by this refactor; useful results and limitations from its handoff are preserved here.

## Imported date ranges

Imported on 2026-09-16; original date ranges are preserved below. They do not imply work occurred on the import date.

- (2026-08-01 → 08-03) Dual-host packaging: surveyed Codex plugins/skills/hooks, merged the split Codex package into one dual-manifest package with a single shared hook implementation and validator drift assertions, then fixed the install — the plugin was discoverable but `not installed`, which the desktop UI hides. Stop signals became advisory on both hosts, and the stale-handoff signal was decoupled from the large-file signal.
- (2026-06-26 → 06-27) Built the plugin: memory files, handoff, conventions, validation, hooks, and lessons; then condensed the core rule and skills, added observable success signals, and made handoff next actions verifiable.
