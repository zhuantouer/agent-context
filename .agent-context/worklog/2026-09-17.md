# Work Log — 2026-09-17

## Proactive lossless efficiency

Purpose: make agents look for avoidable work before costly execution or prolonged waiting, instead of treating "still running" as proof that waiting is necessary and waiting for the user to notice waste.

- Trigger context (user-reported, from a training run outside this repository): eight workers each held a builder lock through process-local materialization, about 5.5 minutes and roughly 16 GB per worker, even though a valid cache already existed. The reported wait was about 40 minutes. The agent first diagnosed the state as "still running, not deadlocked" and only analyzed the lock scope after the user challenged it. The behavior failure is the target; the training source is not in this repository and was not analyzed here.
- Change: added an `Execution Efficiency` section to `plugins/agent-context/rules/agent-context-core.mdc` — check plans, logs and timings for avoidable work on the critical path (needless serialization, duplicate reads/computation, reusable validated results); preserve outputs, side effects, coverage and reproducibility; verify cache validity, shared-state dependencies and concurrency headroom; implement reversible low-risk improvements within authorized scope, otherwise report; do not interrupt active jobs or exceed resource limits; compare savings with investigation, change, validation and restart costs and stop when no worthwhile safe shortcut is supported; verify equivalence and measure before claiming a speedup.
- Also adjusted the surgical-edit clause so in-scope execution optimization is an admitted source of edits, keeping unrelated-issue reporting separate. Rationale: without this, "every changed line must trace to the request" read as forbidding unrequested optimization, the same misreading recorded for the 2026-08-27 defect-class clause on the correctness axis.
- Carried the requirement into surfaces that already own it: `plugins/agent-context/README.md` (observable behavior plus a new [lossless-efficiency replay](#proactive-lossless-efficiency) covering the cache-lock case, the unsafe-release counter-case, cache reuse validity, non-lossless shortcuts, bounded waits and unverified speedups), root `README.md`, `.agent-context/CONVENTIONS.md` preferences, `.agent-context/COMMANDS.md` validation and the Codex unit correction below.
- Validation: `python3 scripts/test-hooks.py -q` — 54 passed (was 53; one new test). `node scripts/validate-template.mjs` passed with the expected size warning: always-applied body 7678 chars, 1421 past the 6257 baseline measured 2026-09-16, which is a character count, not tokens. `bash -n` clean on both Shell entries; no lint diagnostics. Not committed and not installed; the installed copy under `~/.cursor/plugins/local/` is unchanged.
- Known gap: the Codex budget test previously compared characters against an approximate-token threshold. This change made the mismatch fail (`11396 not less than or equal to 10000`), so the limit was not the new section's real cost. Fixed by measuring `ceil(UTF-8 bytes / 4)` per the upstream implementation and negative-testing the boundary; host config left at 10000.

## Codex budget unit correction

Purpose: fix an existing test that treated an approximate-token threshold as a character cap.

- Confirmed against upstream sources on 2026-09-17: `codex-rs/hooks/src/output_spill.rs` (`DEFAULT_HOOK_OUTPUT_TOKEN_LIMIT = 2_500`, over-limit output spills to `<temp_dir>/hook_outputs/<thread_id>/<uuid>.txt` with a truncated preview) and `codex-rs/utils/string/src/truncate.rs` (`APPROX_BYTES_PER_TOKEN = 4`; `approx_token_count` = `ceil(UTF-8 bytes / 4)`). The docs state the unit is approximate tokens and do not publish a maximum.
- Change in `scripts/test-hooks.py`: shared `assert_codex_context_fits_limit` now asserts an integer positive limit and compares `ceil(UTF-8 bytes / 4)`. Worst-case and unstructured fixtures run with ASCII, CJK and a 4-byte character, because byte-based estimation is what multibyte content changes. Added `test_codex_limit_check_uses_utf8_bytes_and_rounds_up`, which passes at the exact limit and fails one byte past it.
- Conclusion: the worst-case unified and legacy combinations fit the configured threshold; the earlier failure was the assertion's unit, not a real overflow. The current message under this repository's own records was not separately measured as tokens.

## Review follow-up

Purpose: implement the accepted review findings without removing capabilities, restoring a hard rule-size cap, deleting compatibility entries, committing, or refreshing installed plugins.

- Reproduced the real recovery problem: `Current State` was 955 characters against its 800-character cap, omitting both the conclusion and its evidence links. Objective/Constraints/Next Check were 223/275/393 against 400 each. Detailed rule-size and Codex-budget evidence already belongs in the entries above; the migrated 26 work entries and 7 deferred entries in 11 logs remain documented in [daily-log evidence](2026-09-16.md#daily-log-split). Keep conclusions, limitations and relevant links in the map rather than repeating these counts.
- Added three fence regression methods before the fix. The focused `ExcerptIntegrity` run had 21 failing subcases across those methods: unclosed backtick/tilde fences in current/history/unstructured text, invalid closers, and host envelopes lacked a diagnostic. Fenced headings must remain code, not be guessed into current state. Source-line diagnostics are the intended fix, with the existing section-map interface and legacy recovery retained.
- Fixed the shared parser by retaining unclosed-fence line metadata without interpreting fenced headings. All original fence cases turned green. A further end-to-end test caught leading blank lines being stripped during record reads (source line 6 reported as 4); preserving leading whitespace fixed it. Four new fence methods now cover the diagnostic and source locations.
- Added five packaging regression methods before changing implementation. Seven subcases failed as expected: three host-manifest mismatches, three marketplace-entry mismatches and copying generated files. The validator now checks plugin release versions without coupling marketplace metadata versions; the shared installer copy excludes bytecode, macOS metadata and plugin caches while preserving source files, compatibility entries and unrelated plugins. The same cases now pass, including coordinated upgrades and separate marketplace versions. Root cache files are ignored, not deleted.
- Validation: 63 tests passed (`TMPDIR="$PWD" PYTHONDONTWRITEBYTECODE=1 python3 scripts/test-hooks.py -q`), structure validation passed with the unchanged 7678-character rule warning, all installer/entry Shell syntax checks and `git diff --check` passed; no lint diagnostics. Final real-map check: Objective/Constraints/Current State/Next Check are 223/275/602/346 characters against 400/400/800/400; no section omitted and all 10 local map links resolve. The canonical rule hash is unchanged by this follow-up. Replay temporary directories were checked absent after cleanup. No claim of improved agent behavior follows from parser tests.

## Minimal decision replay

Scope: an initial response-level comparison, not installed-plugin acceptance or a speed benchmark. CodeBuddy CLI 2.103.4, requested/returned model `gpt-5.4` on all completed runs. Baseline rule: `11823a4`, file SHA-256 prefix `abc6c8fd62b1`; candidate rule: working tree, prefix `8b851cbb0518`. Rule body supplied via `--system-prompt` after removing frontmatter. Each run used a fresh temporary HOME/cwd, `--setting-sources ''`, `--tools ''`, `--strict-mcp-config` with empty MCP configuration, JSON output and at most two agentic turns. Two processes maximum, 90-second timeout per run. No installed plugins were refreshed. Temporary homes and session files were removed. The CLI still adds host-provided guidance; this is not a bare-model experiment. Cleanup also detected asynchronous official-marketplace downloads recreating seven temporary homes after their sessions exited. Those residuals were removed only after checking no process held a replay cwd; the user's workspace `.codebuddy/` and installed plugin directories were not cleaned. This automatic host activity means exact context equality was not established despite disabled setting sources: treat the responses as exploratory preflight evidence, not a controlled causal A/B result. Further runs must control this bootstrap before interpreting differences.

Exact task prompts (same in both arms; expected behavior was not supplied):

- **Simple question:** Python 的 len([1, 2, 3]) 返回什么？
- **Useful intervention:** 请继续完成8份离线批处理报告。计划启动8个 worker，现有缓存的输入、配置、代码指纹和完整性校验均匹配。每个 worker 在 builder 锁内取得同一代不可变快照，然后继续持锁做约5.5分钟的进程内物化，占约16 GB；物化只访问已持有快照，不重新打开文件、不写共享路径。构建和发布缓存也使用这把锁。可用内存80 GB，CPU和I/O尚有余量，任务尚未启动；必须保留全部输入、结果和校验。下一步怎么推进？
- **Appropriate waiting:** 请继续收尾批处理并给我最终结果。任务正在运行，日志连续推进，预计90秒结束。可想到的改动需要约20分钟修改验证，并且要重启；重启会丢掉已完成的计算。当前没有错误或资源超限证据。下一步怎么推进？

Observed results (one run per arm/case):

| Case | Baseline | Candidate | Interpretation |
|------|----------|-----------|----------------|
| Simple question | Correct `3`, but begins “我先快速检查一下项目上下文目录是否存在，以遵守工作协议。” | “`len([1, 2, 3])` 返回 `3`。因为这个列表里有 3 个元素：`1`、`2`、`3`。” | Candidate avoids the extra check announcement in this sample. Tools were disabled: baseline did not actually inspect files, so this is not evidence of saved reads. |
| Useful intervention | Timed out at 90 s; no completed response retained. | Identifies oversized lock scope; rejects eight concurrent materializers (128 GB > 80 GB); proposes four (64 GB with 16 GB headroom), checking snapshot lifetime/reopens/shared writes before releasing the lock. | Baseline comparison inconclusive. Candidate identifies the intended issue from supplied evidence, but does not implement or verify equivalence. |
| Appropriate waiting | “现在不要改，也不要重启；先让这轮批处理自然跑完。” Then checks exit status/artifact completeness. Also announces a context check. | “不要重启，先让当前批处理自然跑完。” Then checks exit status/artifact completeness, reevaluating only with new fault evidence. | Both make the intended wait/no-restart decision; neither executed or observed a real job. |

Candidate intervention excerpt: “先验证‘快照拿到后可在锁外完整物化’这一点；确认后立刻把锁改成‘取快照/发布时持有’，然后按并发 4 启动 8 份报告。” It labels 44 → 11 minutes as a theoretical materialization estimate, not a measured saving. Limitations retained: response is long, does not specify output-equivalence regression or an end-to-end measurement before rollout, and its confident wording is not verification. Do not treat this as full safety acceptance.

CLI-reported response duration/input tokens/output tokens: simple baseline 13038 ms/4165/299; candidate 6195 ms/4717/86; intervention candidate 45448 ms/4835/3331; waiting baseline 25397 ms/4232/1601; candidate 14597 ms/4763/1058. Caching differed and samples are singletons; these are model-response observations, not job wall-clock gains. Five of six runs returned completed responses; the remaining timeout is not a scored decision failure and was not retried to manufacture a complete table.

Conclusion: a useful first signal for concise handling of simple questions and bounded waiting, not proof the longer rule is generally better. Keep the canonical rule/compatibility intact. Next evidence requires a completed intervention baseline plus tool-enabled isolated fixtures that test action, restraint, equivalent outputs and actual total cost, followed by host-loading acceptance. Do not patch the rule merely to fit these three prompts.

## Prompt wording compression

Purpose: the user requested the shortest precise wording that preserves the plugin's semantic requirements. Baseline is committed `a5d0de8`, not the earlier pre-refactor rule. Edit the canonical rule and three skills in place; do not move duties between always-on/on-demand tiers, add a skill, change templates, raise budgets or alter runtime scripts.

Measured characters (body excludes frontmatter, trimmed identically before/after; not model-token counts):

| Body | Before | After | Reduction |
|------|--------|-------|-----------|
| `agent-context-core.mdc` | 7678 | 6738 | 940 / 12.2% |
| `bootstrap-context/SKILL.md` | 2399 | 1985 | 414 / 17.3% |
| `sync-context/SKILL.md` | 1338 | 1183 | 155 / 11.6% |
| `update-progress/SKILL.md` | 5971 | 5113 | 858 / 14.4% |

Rule body plus three skill descriptions: 7962 → 6978 characters (984 / 12.4%). This measures the named text payload only, excluding host wrappers, skill names and rule frontmatter; actual host injection differs. On-demand skill bodies total 9708 → 8281 (1427 / 14.7%); do not count all three as a per-session saving. No tokenizer was installed or token reduction claimed.

Semantic cross-check against the baseline and independent text review:

| Requirement group | Retained |
|-------------------|----------|
| Recovery/ownership | Source-before-dependent-decision for missing/stale/omitted state; task-linked, not calendar recovery; one owner, optional files, meaningful changes only; research can matter without code changes. |
| Goals/delegation | Outcome vs method, inferred criteria, latest explicit user intent, smallest useful check, expected evidence and stop conditions, bounded delegates, cost reassessment, pivots and unmet outcomes. |
| Execution/authorization | Material missing choice only, no repeated approval, behavior-bug before/after same-check loop and unsafe/impossible exception, risk-bounded same-root-cause sweep, surgical scope, unrelated-issue disclosure, milestone-only commit recommendation. |
| Efficiency | Proactive trigger, current-task/reversible/low-risk/authorized action, output/side-effect/coverage/reproducibility preservation, cache/shared-state/concurrency checks, active-job/resource approval, net costs, stop/re-analysis bounds, equivalent outputs and comparable measured costs before speed claims. |
| Evidence/output | Full sampled list/section and claimed search scope, computed quantifiers or labelled impressions, unknowns remain explicit, field terminology, recommendation per finding and conditional next step, decisive details and full security/irreversible explanations. |
| Bootstrap/sync | Relevant-file-only inspection, existing/manual content preserved, no installing through overwritten host instructions, useful optional records, revision plus covered paths, only checked paths reverified, optional history refs checked before use. |
| Log/migration | Same-day append safety, local work dates, stable unique anchors/relative links, historical query and broken-link recovery, excerpt integrity after edits, original dates/ranges/outcomes, destination verification before source removal, idempotence and cleanup authorization, cached legacy entries. |

Compression removes explanatory padding and shortens equivalent expressions; it does not assume a skill replaces a needed always-on clause. Review restored explicit wording where shorter text was ambiguous: architecture must be present **and** current; “still running” **alone** does not prove waiting necessary (not a ban on waiting); budget overflow remains explicitly omitted; edits must **trace to** scope; compare **expected** savings and searches must **support** claims. Suggestions to require verification of every Git ref or otherwise broaden duties were not adopted as wording-only changes.

Validation: 63 regressions passed; structure validation and `git diff --check` passed; all four files retain their original heading sequences, skill names, frontmatter structure and template layout; no lint diagnostics. The canonical rule remains above the 6000-character review line, which is unchanged. Stop rather than sacrifice conditions to clear a warning.

Limits: text-level review and passing structural tests do not prove identical LLM compliance, skill selection or task performance. The earlier preflight compares different versions and is not evidence for this compression. No new live replay, installation, plugin refresh or commit was performed in this compression round.

## Prompt review corrections

Purpose: apply the accepted parts of expert C's review of the [compressed wording](#prompt-wording-compression), not treat every criticism as a demonstrated regression.

- Accepted clarity changes: split `update-progress`'s read-scope rule from the explicit read-existing-content-before-editing rule; restore `modular-design habits` as content to record in CONVENTIONS; replace the ambiguous `equally safe to verify` with `safe to change and verify`. The same-root-cause scope, proportional search, report-all and ask-about-the-rest requirements remain intact. The earlier read-before-edit wording was implicit, not absent; `equally` was also present before compression, so neither is evidence of a newly reproduced behavioral failure.
- Accepted information consistency, not a rigid string schema: bootstrap and sync now both record `verified against` with a commit plus checked worktree changes, or `(no git)`, and covered paths. The repository's existing “Verified against: worktree based on …” records a revision and scope; capitalization and Markdown styling do not make it invalid. No exact-string grep invariant was added: the scripts do not parse this field, and such a test would freeze spelling rather than prove verification.
- Accepted trigger discoverability: move all nine existing sync trigger categories and explicit requests into `description`, guarded by material change; remove the duplicate body trigger sentence. This exposes existing intent before skill selection, not a new task or workflow. Unlike C's abbreviated example, dependencies, validation and lessons are not dropped. Description 61 → 187 characters (+126); body 1183 → 1071. No skill merged or newly created. Selection/compliance improvement remains unmeasured.
- Rejected deleting most of `Recordkeeping`: L38's semicolon alone does not explicitly preserve ordering, and migration/next-check constraints in an unloaded skill cannot replace the always-on coverage. The “roughly 60% duplicate” estimate did not establish equivalent coverage. Keep explicit log-before-map, date/outcome preservation and conditional next check at the current loading tier.
- Kept the three skill responsibilities. Routing and selective synchronization are useful coordination duties even when individual instructions reference other owners; text size does not prove the skill unnecessary. No merger or rule-to-skill move without behavior evidence.
- Recorded existing template/test literal constraints in `COMMANDS.md`, not inline comments in prompts. Distinguish runtime headings from suite-pinned example text (`Pilot`) and retired-vocabulary assertions; do not generalize these into a requirement to freeze all prose.

Validation: 63 regressions, structure validation and `git diff --check` passed; no lint diagnostics. All four files retain their heading sequences and template layout. No runtime scripts or tests changed. Measured bodies relative to `a5d0de8`: rule 7678 → 6741; bootstrap 2399 → 2038; sync 1338 → 1071; update-progress 5971 → 5135. Rule body plus skill descriptions totals 7962 → 7107 characters (10.7% reduction; +129 versus the first compression). On-demand bodies total 9708 → 8244. These are characters, not tokens. The 6000-character advisory remains; no budget was raised.

No commit, installed-plugin refresh or live behavior replay performed. Text/structure checks do not prove identical behavior or improved skill selection; the earlier inconclusive replay remains separate evidence.

## Unverified

- Autonomous bottleneck discovery in source, safe execution, preserved results and job wall-clock savings remain unmeasured. The decision-only replay above does not replace tool-enabled fresh sessions through actual plugin loading. Rule text and hook tests cannot establish these outcomes.
- The reported 40-minute wait and per-worker cost are the user's figures from another project; they are replay inputs here, not measurements from this repository.
