# Work Log — 2026-09-21

## Review of the direction-change objective-rewrite patch

Purpose: a real incident showed a mid-conversation goal change never rewrote `PROGRESS.md ## Objective` (hand-written state files bypassed `update-progress`, so no criterion forced the rewrite). Another expert implemented the six proposed fixes (A–F) as uncommitted working-tree changes; the user asked for review before commit.

- **Coverage verified — all six proposals landed and correctly layered**: A (same-turn overwrite + no parallel old yardstick) `core.mdc` L29–35; B (milestone re-anchor, "skipping Objective counts as unfinished") L70; C (three-part live-field Objective, overwrite-only) core L102 + `update-progress` template; D (restored goal must land in PROGRESS, not just design docs) L37; E (state writes must load the skill, no hand-writing) core L110 + both SKILL.md files, with `bootstrap-context` delegating drift back to `update-progress`; F (goal check in peer review) L72.
- **Blocking — regression suite is red**: `python3 scripts/test-hooks.py` fails 1/32 (`test_template_partitions_resume_and_history`). The new `## 已回答的问题` heading breaks the template's own contract ("标题保持英文", `update-progress` L32 — all six existing template headings are English) and the closed heading partition the test enforces.
- **Semantic defects**: (1) "被取代" goals are routed into `## 已回答的问题`, but superseded goals are often unanswered — recording them as answered creates false records; the template placeholder itself says "已**结案**", inconsistent with its own heading. Fix: English `## Closed Questions` with per-entry status (`已判负` / `已回答` / `被取代，未回答 + revisit condition`). (2) The mandatory same-turn overwrite (core L29) has no substance threshold, conflicting with the Startup carve-out ("简单问答…不写项目记忆"): a side question would evict the main objective from `## Objective` — a new drift source. Fix: trigger only when the new question becomes the working goal; side questions go to worklog/`Deferred`.
- **Injection budget**: rule body 3418 → 4272 chars (+25%) against the 6000-char host limit (`test_accepts_always_applied_rule_at_the_host_injection_limit` still passes); headroom ~2580 → ~1728. About 250 chars are incident-specific narrative (core L35 example, L110 parenthetical) — a permanent per-turn cost across all projects; keep or trim is a conscious tradeoff, not an oversight.
- **Verified non-issues**: `session-context.py` reads the mdc body directly, so the change propagates to the Codex host without code edits; README and `legacy-migration.md` do not describe template details; the seven rule section headings are unchanged so `test_canonical_rule_keeps_the_expected_sections` passes.
- **Dogfooding observation**: loading `update-progress` in-session returned the installed copy at `~/.codebuddy/plugins/agentic-protocol`, which is still the pre-patch version — the working tree is ahead of the installed plugin; hosts will not see these rules until reinstall.
- Review performed under the new rules themselves: goal re-anchor done — the repo Objective ("Keep agent-assisted research goal-directed across sessions") still describes this work; no rewrite needed.

Conclusion: the patch direction and layering are right (norm in core, procedure in skill, ownership respected), but it is **not commit-ready**: fix the English closed-questions heading plus per-entry status, add a substance threshold to the mandatory rewrite, sync `scripts/test-hooks.py`, and re-run the suite to green before committing. Minor wording fixes (double "先" in update-progress instruction 2, vague "视为未完成" enforcement, `bootstrap-context` hand-back ambiguity, quote style) can ride along; the incident-narrative budget spend and migrating this repo's own free-form Objective to the new three-part template are user decisions.

## Fixes applied to the direction-change patch

Purpose: the user accepted findings 1–3 and added a real constraint the single-slot model missed — **several goal lines run in parallel, so switching is not closing**; they also asked to cut the incident-specific narrative and the overall size.

- **Goal model is now three partitions, not one slot**: `## Objective` holds only the *active* line (the only active yardstick) and is overwritten on switch; `## Other Goals` holds the rest with status (`并行推进` / `暂停` / `等待前置`) plus a resume condition (≤3), promoted back to Objective only when named; `## Closed Questions` holds closed lines with status (`已回答` / `已判负` / `被取代`) plus "不得再用该口径解读". All headings are English, which is what the template contract required.
- **Substance threshold on the mandatory rewrite**: it now fires only when the new question *becomes the working goal* (drives subsequent actions or the user confirms continuation); side questions fall through to worklog/`Deferred`. This also gives the clause the "off switch on trivial work" the validator's audit asks for.
- **Test contract updated**: `scripts/test-hooks.py` gains `OPEN_GOAL_SECTIONS` / `CLOSED_GOAL_SECTIONS`; the heading-partition assertion now includes both, so a future section cannot slip in silently.
- **Trimmed to 4190 chars** (G's version 4272, HEAD 3418): deleted the baseline/scaling incident example, the "one real failure" parenthetical, the assumption reminder duplicated by the existing Goal Alignment line, and the goal-routing text duplicated by the Project Memory table; merged the direction-change and multi-line paragraphs; compressed the peer-review clause. `node scripts/validate-template.mjs` now passes **without** the 4200-char review warning.
- Validation: `python3 scripts/test-hooks.py` **32/32 OK**; validator clean; `session-context.py` still reads the mdc body directly so no code change was needed.

Conclusion: the patch is commit-ready on the review findings — parallel goal lines no longer collapse into "closed", the rewrite trigger no longer fires on side questions, and the always-injected rule fits the budget again. Still open for the user: whether to trim further (each further cut would remove behaviour this patch exists to enforce), migrating this repo's own free-form Objective to the three-part template, and reinstalling (`./scripts/install-local.sh all`) so the hosts stop running the pre-patch copy.
