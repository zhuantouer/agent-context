# Work Log — 2026-09-22

## Expert A review of goal and evidence semantics

Purpose: review the current core rule and both skills for further improvements, without editing those sources. Reviewed HEAD `f5e71f9`, initially clean. The repository Objective still describes this work: preserve goal-directed decisions and evidence without duplicate bookkeeping. Assumed acceptance: actionable inconsistencies with concrete scenarios and minimal remedies, not a larger framework.

- **P1 — focus is still conflated with goal identity** (`plugin/rules/agentic-protocol-core.mdc:27,51`; `plugin/skills/update-progress/SKILL.md:13,36–43`). One Objective plus Other Goals records other lines, but provides no explicit parent/child or per-line judgment rule. Counterexample: two subgoals run concurrently under one objective; handling the second result can overwrite the parent or apply the first line's criterion. Recommendation: distinguish execution focus from changing a goal; preserve the parent and each active line's acceptance criteria/status, and replace obsolete wording only within the affected line. This addresses the user's parallel-goal requirement; it does not require a full goal tree.
- **P2 — unchanged map can suppress useful evidence** (`update-progress:12–14,19–25`). The unconditional “状态未变则不写文件” precedes independent worklog criteria. Counterexample: new reproducible verification supports an unchanged conclusion. Recommendation: evaluate map changes and worthwhile evidence separately; skip all writes only when neither applies.
- **P2 — superseded is still a closure status** (`update-progress:45–46`, versus `:13` and core `:27`). The template still lists 被取代 in Closed Questions even though switching an unfinished line should preserve it. It also assumes every closed question has a forbidden old criterion; a valid answered question need not have an obsolete criterion. Recommendation: distinguish actual closure from switching/supersession; mark only genuinely superseded criteria inapplicable, scoped to the relevant goal and conditions.
- **P2 — compact-map constraint incorrectly covers all context** (core `:77`, versus `update-progress:81–84`). Requiring all of `.agent-context/` to remain fully rereadable pressures historical evidence to be discarded as it grows. Recommendation: apply that bound to `PROGRESS.md`; read knowledge and evidence on demand.
- `bootstrap-context` ownership and delegation are coherent in this pass. Its File contents list duplicates the core ownership table; optional compression can retain only unique extraction/verification/delegation instructions. Defer broader restructuring.
- Validation: `python3 scripts/test-hooks.py` passed 32/32; `node scripts/validate-template.mjs` passed without warnings, combined exit 0. These are structure/packaging checks, not semantic behavior acceptance. Static review of tests and README found no automated behavior checks for concurrent open goals, evidence-only writes or superseded-but-open goals; existing manual replay cases are adjacent but insufficient. No live-host behavioral replay was performed, so the counterexamples above are rule-level risks, not measured agent failures.

Conclusion: replace ambiguous clauses rather than add incident narratives. Before treating a revision as behaviorally accepted, exercise concurrent parent/subgoal work, focus switch without closure, evidence-only writes, and a trivial side question that leaves the main objective untouched. No source edits, commit, installation or reload were performed in this review. Prior fix evidence remains in [the September 21 log](2026-09-21.md#fixes-applied-to-the-direction-change-patch); this review supersedes its broad commit-ready/behavior-complete assessment.

## Implemented goal ownership and independent evidence writes

Purpose: the user approved implementing the review recommendations. The parent project objective is unchanged; this work addresses the bounded goal/evidence semantics gap rather than introducing a new framework.

- Replaced single-slot goal switching with per-goal updates: preserve parent/parallel ownership and each criterion; focus switches only alter task references. Actual goal/criterion changes update the affected definition in the same turn, with prior wording and reasons retained as evidence. Milestone/review re-anchoring and bootstrap drift checks use the owning goal, not necessarily Objective.
- Kept the existing headings and two skill boundaries. Other Goals now explicitly retains ownership, acceptance and status; Closed Questions retains valid criteria and scope for later rechecks. Only answered questions or explicit user abandonment close; focus switching, pausing or a failed method does not. Compaction applies to PROGRESS, not the evidence archive. Removed bootstrap's duplicate ownership list.
- Split map changes from evidence write criteria, preserving reusable new validation even when conclusions do not change. Independent read-only review of eight scenarios identified an additional conflict in the old “无新结论的重复检查” exclusion and missing parent/criterion fields in migrated or closed entries; these were corrected before final validation. The reviewer did not read README expectations, write task fixtures or run a host; this was static scenario reasoning only.
- Red/green evidence: four new text-contract tests failed on the pre-edit rule/templates (five assertion failures including two subtests), then the complete suite passed 36/36. `node scripts/validate-template.mjs` passed without warnings; `git diff --check` passed. IDE diagnostics reported no issues on the edited source/test/docs. Tests protect selected text/template contracts, not acting-agent behavior.
- Added eight fixed-input manual cases in [Goal and evidence replay](../../plugin/README.md#goal-and-evidence-replay): concurrent child result, focus-only switch, evidence-only write, side question, scoped criterion revision, replacing the parent while preserving open children, reopening on a new build, and explicit abandonment. Corrected the adjacent test-coverage claim to avoid implying Python tests prove these behaviors.
- Always-applied rule body: HEAD 3161 → candidate 3188 characters (+27). Detail stays in the on-demand skill and acceptance cases; no incident narrative or new runtime dependency was added.

Conclusion: the reviewed textual defects are addressed and local checks pass; live-host behavior remains unverified. No installation, reload, commit or push was performed. Before claiming behavioral acceptance, use isolated tool-enabled sessions and inspect actual record changes for the documented cases. The installed skill loaded during this turn still supplies pre-edit text; it was not refreshed.
