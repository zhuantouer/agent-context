---
name: bootstrap-context
description: Initialize the .agent/ context directory for this project.
---

Run the bootstrap-context skill to scan the project and generate `.agent/` context files (ARCHITECTURE.md, COMMANDS.md, CONFIG.md, PROGRESS.md, DECISIONS.md).

This is safe to run on any project. It only creates the `.agent/` directory and does not modify any existing project files.
